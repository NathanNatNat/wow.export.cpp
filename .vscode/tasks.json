{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "cmake",
            "command": "build",
            "label": "CMake: Build",
            "group": { "kind": "build", "isDefault": true },
            "detail": "Build the active CMake preset"
        }
    ]
}
