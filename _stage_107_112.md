## File: src/js/components/item-picker-modal.cpp (audit entry #107)

FINDING: Missing `is_loading` / `load_error` state and async DBItemList.initialize() call
  JS Source: src/js/components/item-picker-modal.js lines 37-38, 107-119
  Details: The JS component tracks `is_loading` and `load_error` reactive flags and, when
  `slot_id` changes and `all_items` is empty, calls `await DBItemList.initialize()` to lazy-
  load items. The C++ render() has no equivalent: it simply reads `tab_items::getAllItems()`
  every frame. If items are not yet loaded the modal silently shows "Loading items..." but
  never triggers the load. The missing flags also mean that the "Failed to load items."
  error branch (JS template line 169) is not rendered at all.

FINDING: `filter_text.trim()` not applied before substring search
  JS Source: src/js/components/item-picker-modal.js line 65
  Details: The JS `filtered_items` computed trims the filter text before lowercasing
  (`this.filter_text.trim().toLowerCase()`). The C++ `rebuild_filtered` lowercases
  `s_filter_buf` directly without trimming leading/trailing whitespace first. This means
  a filter of "  sword  " would not match "sword" in C++ but would in JS.

FINDING: Item name rendered using `item->name` instead of `item->displayName`
  JS Source: src/js/components/item-picker-modal.js line 173
  Details: The JS template renders `{{ item.name }}` (the display name field) for the item
  label. The C++ Selectable label is built as `item->name` (line 240), but in the
  `tab_items::ItemData` struct `name` is the raw DB name. The JS filtering also searches
  `item.displayName` (line 65). The C++ filter searches `item->displayName` correctly (line
  104) but displays `item->name`. These fields may differ; display should use `displayName`
  to match JS behaviour.

FINDING: Icon rendering in the item list row is missing
  JS Source: src/js/components/item-picker-modal.js lines 171-172
  Details: The JS template renders `<div :class="['item-icon', 'icon-' + item.icon]"></div>`
  for each row. The C++ code pre-loads icons via `icon_render::loadIcon()` (correct) but
  does not render the icon image alongside each item row in the Selectable. The icon texture
  should be drawn (e.g., via `ImGui::Image`) before the item name, matching the JS layout.

FINDING: `open_items_tab` emits `'open-items-tab'` event; C++ calls `modules::set_active` directly
  JS Source: src/js/components/item-picker-modal.js lines 143-145, 179
  Details: In JS the "Search in Items Tab" link emits `'open-items-tab'`, letting the parent
  handle tab switching. The C++ version directly calls `modules::set_active("tab_items")`
  inside the modal component. While functionally equivalent in the current app, it breaks the
  JS component boundary: the modal should not know about the module system. This is a minor
  fidelity deviation; document it.

FINDING: Escape key handling — C++ checks after EndChild, JS handler is global
  JS Source: src/js/components/item-picker-modal.js lines 148-151, 138-140
  Details: The JS mounts a `keydown` listener on `document` in `mounted()` which fires for
  Escape at any time. The C++ checks `ImGui::IsKeyPressed(ImGuiKey_Escape)` after `EndChild`
  inside the popup, which only fires when the popup window has ImGui focus. This is a minor
  difference but the user can close the modal with Escape in JS even when focus is not in the
  popup window.

FINDING: Overlay click-to-dismiss not implemented
  JS Source: src/js/components/item-picker-modal.js line 161
  Details: The JS template wraps the modal in `<div class="item-picker-overlay" @click.self="$emit('close')">`,
  so clicking outside the modal content (on the semi-transparent overlay) closes it. The C++
  uses `ImGui::BeginPopupModal` which also closes on background click by default when the
  `modal_open` pointer arg is provided — this is handled. However the overlay backdrop (a
  full-screen semi-transparent div) is not drawn, meaning there is no visual dimming of the
  background, which differs from the JS UI.

AUDIT_TRACKER_CHECK: 107

---

## File: src/js/components/itemlistbox.cpp (audit entry #108)

FINDING: `emits` list in JS includes `'options'` — C++ exposes `onOptions` callback but JS only emits `'equip'`
  JS Source: src/js/components/itemlistbox.js line 20
  Details: The JS `emits` array is `['update:selection', 'equip']` — there is no `'options'`
  event in the JS source. The C++ header declares `onOptions` callback and the render
  function renders an "Options" button that invokes it. This extra "Options" button and
  callback is not present in the JS source and represents a deviation.

FINDING: Item height hardcoded to 46px but JS uses `child.clientHeight` (26px items in practice)
  JS Source: src/js/components/itemlistbox.js line 155
  Details: The JS `resize()` uses `Math.floor(this.$refs.root.clientHeight / 26)` (26px slot
  height, same as the base listbox). The C++ comment says `// CSS: #tab-items #listbox-items
  .item { height: 46px; }` and uses 46.0f for `itemHeightVal` and the slot count divisor in
  `resize()`. However the actual rendered row height in Dear ImGui will be the default font
  line height (~17-19px), not 46px or 26px. This causes slot count calculation to be wrong
  relative to actual visible rows.

FINDING: Keyboard copy (Ctrl+C) copies `displayName` but JS joins the raw selection items
  JS Source: src/js/components/itemlistbox.js lines 228 (listbox.js reference)
  Details: The JS `handleKey` Ctrl+C copies
  `nw.Clipboard.get().set(this.selection.map(e => e.displayName).join('\n'), 'text')` — the
  `displayName` property. The C++ `handleKey` looks up the item by ID in filteredItems and
  copies `filteredItems[idx].displayName` (line 281). This is correct. No issue.

FINDING: Font scaling hack for item ID span is incorrect use of `ImGui::GetFont()->Scale`
  JS Source: src/js/components/itemlistbox.js line 335 (visual: `font-size: 0.8em` CSS)
  Details: The C++ attempts to render the item ID in a smaller font by modifying
  `ImGui::GetFont()->Scale` directly (lines 582-588). Modifying `Scale` on the shared font
  object is not thread-safe and affects all subsequent text in the same frame until restored.
  The correct approach is to push a separate smaller font or accept the same font size. The
  current code also calls `ImGui::PushFont(ImGui::GetFont())` after modifying Scale, which
  is unusual. While it likely works, it is fragile.

FINDING: `includefilecount` prop is declared in comments but never passed or used
  JS Source: src/js/components/itemlistbox.js line 19
  Details: The JS `props` list includes `'includefilecount'`. The C++ `render()` signature
  and header do not include an `includefilecount` parameter. The JS uses it together with
  `unittype` to conditionally show the file counter. In the C++ code `unittype` alone drives
  the status line at the bottom — the separate `includefilecount` gate is absent.

FINDING: Selectable width calculation subtracts fixed 120px regardless of icon presence
  JS Source: src/js/components/itemlistbox.js lines 333-338 (template)
  Details: The C++ Selectable is given `ImVec2(availSize.x - 120.0f, 0.0f)` to leave room
  for the Equip/Options buttons. This is a hardcoded magic number that may not match actual
  button widths. The JS layout is CSS-driven and auto-sizes. Not a critical functional issue
  but a layout inaccuracy.

AUDIT_TRACKER_CHECK: 108

---

## File: src/js/components/listbox-maps.cpp (audit entry #109)

FINDING: `expansionFilter` watch calls `recalculateBounds()` in JS but C++ only resets scroll/scrollRel
  JS Source: src/js/components/listbox-maps.js lines 27-30
  Details: The JS `expansionFilter` watcher sets `this.scroll = 0`, `this.scrollRel = 0`,
  and then calls `this.recalculateBounds()`. The C++ equivalent (lines 91-95) sets
  `state.base.scroll = 0.0f` and `state.base.scrollRel = 0.0f` but does not call
  `recalculateBounds`. In the JS, `recalculateBounds` clamps scroll and recomputes
  scrollRel from the current DOM measurements; since scroll is already 0 it is a no-op in
  that specific case, so the functional impact is minimal. However, the C++ also does not
  save the updated scroll position via `persistscrollkey` (which `recalculateBounds` does in
  the base listbox). If a persistscrollkey is set, the scroll position is not cleared in
  storage when the filter changes.

FINDING: JS uses `this.itemList` (which resolves the `override` prop) before expansion
  filtering; C++ resolves override before expansion filtering too — but the `overrideItems`
  pointer path passes `nullptr` to the base render, losing the override entirely if it was
  not empty
  JS Source: src/js/components/listbox-maps.js lines 44, 37
  Details: The JS `filteredItems` starts from `this.itemList` which is `override?.length > 0 ?
  override : items`. Then applies expansion and text filters. The C++ resolves override vs
  items at lines 99-101 into `resolvedItems`, applies expansion filtering to get
  `expansionFiltered`, then calls `listbox::render(..., nullptr, ...)` (passing `nullptr` for
  overrideItems). This is correct — the override has already been folded into
  `expansionFiltered`. No issue with the resolution logic itself.

NO_ISSUES (beyond the recalculateBounds/persistscrollkey minor deviation noted above)

AUDIT_TRACKER_CHECK: 109

---

## File: src/js/components/listbox-zones.cpp (audit entry #110)

FINDING: `expansionFilter` watch calls `recalculateBounds()` in JS but C++ only resets scroll/scrollRel
  JS Source: src/js/components/listbox-zones.js lines 27-30
  Details: Same deviation as listbox-maps: the JS watcher calls `this.recalculateBounds()`
  after zeroing scroll/scrollRel; the C++ does not. Functional impact is minimal for the
  scroll-reset case but the `persistscrollkey` position is not cleared on filter change.

FINDING: Identical filterByExpansion implementation duplicated from listbox-maps.cpp
  JS Source: src/js/components/listbox-zones.js (entire file mirrors listbox-maps.js structure)
  Details: The `filterByExpansion` function in listbox-zones.cpp (lines 44-64) is byte-for-byte
  identical to the one in listbox-maps.cpp. The JS sources are also identical in structure.
  This duplication is faithful to the JS (each component is a separate module), but could be
  extracted to a shared helper. Since the JS source duplicates it, this is not a fidelity
  issue, only a code quality observation.

AUDIT_TRACKER_CHECK: 110

---

## File: src/js/components/listbox.cpp (audit entry #111)

FINDING: `handlePaste` replaces selection additively instead of replacing completely
  JS Source: src/js/components/listbox.js lines 314-317
  Details: The JS `handlePaste` filters clipboard lines against `this.itemList`, then
  creates a new selection with `newSelection.splice(0); newSelection.push(...entries)` —
  fully replacing the existing selection with only the pasted entries. The C++ `handlePaste`
  (lines 186-203) filters clipboard lines against `sourceItems` and calls
  `onSelectionChanged(entries)` — passing only the pasted entries as the new selection.
  This is functionally correct (it fully replaces the selection). No issue.

FINDING: `handleContextMenu` — JS does `newSelection.splice(0); newSelection.push(item)` (clears
  all then adds one); C++ creates a new single-element vector
  JS Source: src/js/components/listbox.js lines 487-491
  Details: The JS handleContextMenu always replaces selection with `[item]` when the item is
  not already selected. The C++ does the same. No functional difference. No issue.

FINDING: `renderStatusBar` uses raw `ImDrawList::AddRectFilled` for the status bar background
  JS Source: src/js/components/listbox.js lines 510-514 (template, CSS .list-status)
  Details: Per CLAUDE.md guidelines, raw ImDrawList calls should not be used for things a
  native widget can handle. The status bar background is drawn with `AddRectFilled`
  (listbox.cpp lines 813-816). This violates the project's rendering guidelines — a child
  window or styled region should be used instead.

FINDING: Quick filter rendering uses `ImGui::Selectable` to simulate hyperlinks
  JS Source: src/js/components/listbox.js lines 512-513 (template quick-filters)
  Details: The JS renders quick filters as `<a>` elements with CSS hover underline. The C++
  uses `ImGui::Selectable` styled with `PushStyleColor` to simulate link appearance (lines
  846-849). This is a reasonable approximation given ImGui constraints, but the hover
  underline (CSS `text-decoration: underline`) is not implemented at all.

FINDING: Filter debounce uses `std::chrono::steady_clock` polled every frame; initial state
  sets `debouncedFilter` to filter immediately but subsequent changes go through debounce
  JS Source: src/js/components/listbox.js lines 139-146, 78-79
  Details: The JS `mounted()` sets `this.debouncedFilter = this.filter` (immediate, no
  debounce) and the `filter` watcher applies a 200ms debounce. The C++ does the same: the
  first frame sets `debouncedFilter = filter` (line 602-604) and subsequent changes are
  debounced. This is correct. No issue.

FINDING: `scrollPositionRestored` flag prevents re-restoration when `filteredItems` changes
  JS Source: src/js/components/listbox.js lines 148-160 (filteredItems watch)
  Details: The JS `filteredItems` watcher restores scroll position when items first become
  non-empty, setting `scrollPositionRestored = true` after the first restore. The C++ checks
  `!state.scrollPositionRestored` (line 634) which behaves the same way. However, the JS
  checks both `!this.scrollPositionRestored` and `newItems.length > 0` inside the watcher,
  while the C++ checks both conditions in the same block. Functionally equivalent.

FINDING: `listbox.cpp` status bar renders bold text via a comment but no font push
  JS Source: src/js/components/listbox.js line 510 (`font-weight: bold`)
  Details: The CSS `.list-status` has `font-weight: bold`. The C++ `renderStatusBar`
  comments `// Render bold status text` (line 819) but uses `ImGui::TextUnformatted` without
  pushing a bold font variant. Bold rendering is not implemented.

AUDIT_TRACKER_CHECK: 111

---

## File: src/js/components/listboxb.cpp (audit entry #112)

FINDING: JS selection stores item *objects*; C++ stores item *indices* — Ctrl+C copies labels
  differently
  JS Source: src/js/components/listboxb.js lines 179-181
  Details: In JS, `this.selection` contains the actual item objects (e.g., `{label: "..."}`)
  and Ctrl+C copies `this.selection.join('\n')`. Since items are objects, `.join('\n')` would
  produce `[object Object]\n[object Object]` unless items have a custom `toString()`. The C++
  looks up each selected index to get `items[idx].label`. The JS copy behaviour appears to be
  a JS bug or relies on custom toString. The C++ is actually more correct, but it deviates
  from the literal JS.

FINDING: `wheelMouse` does not respect `scrollSpeed` config
  JS Source: src/js/components/listboxb.js lines 153-162
  Details: The JS `wheelMouse` in listboxb uses a plain
  `Math.floor(this.$el.clientHeight / child.clientHeight)` for scroll count, without checking
  `core.view.config.scrollSpeed`. The base `listbox.cpp` *does* check `scrollSpeed`. The
  `listboxb.cpp` `wheelMouse` also does not check `scrollSpeed` (line 123), which is
  consistent with the JS. No issue.

FINDING: `handleKey` `lastSelectItem` sentinel is -1 in C++ but `null` in JS
  JS Source: src/js/components/listboxb.js lines 175-177
  Details: The JS checks `if (!this.lastSelectItem)` which is falsy for `null` but also for
  index 0 (since `!0` is `true`). This means in JS if the first item (index 0) was last
  selected, pressing arrow keys would be blocked — this is a JS bug. The C++ checks
  `state.lastSelectItem < 0` which correctly allows navigation when item 0 is selected.
  The C++ behaviour is more correct but intentionally deviates from the JS bug.

FINDING: JS `selectItem` and keyboard navigation store item *objects* in `lastSelectItem`;
  C++ stores item indices — fundamental model difference in listboxb
  JS Source: src/js/components/listboxb.js lines 230-270, 192-215
  Details: The JS listboxb stores actual item objects in `this.selection` and
  `this.lastSelectItem`, and uses `.indexOf(item)` to find them. The C++ stores item indices
  (integers) in `selection` and `lastSelectItem`. For `selectItem`, the JS receives the item
  object and `this.selection.indexOf(item)` finds it by reference. The C++ receives the
  index directly (`itemIndex`). The shift-range selection in JS iterates
  `this.items.slice(lowest, lowest + delta + 1)` and pushes item objects; the C++ pushes
  indices. This is a fundamental model difference — whether callers of the C++ API use item
  objects or indices matters for integration correctness.

AUDIT_TRACKER_CHECK: 112

---
