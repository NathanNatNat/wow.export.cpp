## File: src/js/components/map-viewer.cpp (audit entry #113)

FINDING: checkTileQueue uses a per-frame tile limit instead of a concurrency limit
  JS Source: src/js/components/map-viewer.js lines 192–215
  Details: The JS version drains the queue while `state.activeTileRequests < state.maxConcurrentTiles` (up to 4 concurrent async requests). The C++ version replaces this with a per-frame cap of MAX_TILES_PER_FRAME=3 tiles processed synchronously, with a comment explaining the deviation. The comment at lines 101–107 acknowledges this but the semantic difference means the C++ version processes tiles more aggressively per frame than intended — the JS version respects in-flight request counts, while C++ just counts per-frame. The rationale given (single-threaded ImGui loader) is valid and the comment is present, so this is an acceptable documented deviation, but the deviation is not trivial: the JS concurrency limit is 4, the C++ per-frame limit is 3, and the C++ version never uses `activeTileRequests` as a gate in checkTileQueue.

FINDING: tileHasUnexpectedTransparency differs significantly from JS implementation
  JS Source: src/js/components/map-viewer.js lines 287–331
  Details: The JS version calls `ctx.getImageData(left, top, width, height)` on the actual canvas, clamping tile bounds to the canvas rectangle (so it checks only the portion of the tile that's actually on-screen). The C++ version looks up `s_state.tilePixelCache[index]`, which stores the full tile data at load time, and samples from (0,0) to (tileSize-1, tileSize-1) — the full tile rather than the canvas-clamped sub-region. This means the alpha checks occur on different coordinates: the JS version checks the rendered (possibly clipped) portion, while C++ checks the stored full tile. A tile that is partially off-canvas will still return `false` in C++ even though the visible portion may have been clipped incorrectly.

FINDING: setToDefaultPosition does not call render() in all paths (mask fallback path)
  JS Source: src/js/components/map-viewer.js lines 421–462
  Details: In JS, `setToDefaultPosition` calls `this.render()` both when a valid bounding box is found in the mask AND in the fallback path (center on grid center, line 461). In C++, `setToDefaultPosition` never calls `render()` — instead, the caller (`renderWidget`) calls `render()` explicitly after `setToDefaultPosition`. This is functionally equivalent for `renderWidget`, but any direct caller of `setToDefaultPosition` that expects the render to be triggered inside the function will be surprised. The JS contract includes an implicit render call.

FINDING: renderWithDoubleBuffer does not blit the double-buffer canvas back to the main canvas
  JS Source: src/js/components/map-viewer.js lines 554–627
  Details: JS lines 624–626 clear the main canvas and blit the double-buffer onto it: `ctx.clearRect(0, 0, ...); ctx.drawImage(state.doubleBuffer, 0, 0);`. In C++, this step is acknowledged in a comment at line 576–579 but not implemented (there is no GL equivalent). The comment notes it is preserved "structurally" but the actual pixel-level copy does not happen. This is a known structural difference (GL textures are repositioned by offset rather than pixel-blitted), documented in the comment but the deviation is significant.

FINDING: renderFullRedraw in C++ also clears tilePixelCache and tileTextures before redraw, but JS only calls ctx.clearRect and state.rendered.clear()
  JS Source: src/js/components/map-viewer.js lines 632–677
  Details: JS `renderFullRedraw` at line 640–641: `ctx.clearRect(0, 0, canvas.width, canvas.height); state.rendered.clear();`. The C++ version additionally deletes all GL textures, clears tileTextures, and clears tilePixelCache (lines 596–605). This is necessary for the GL approach (textures must be freed) and is functionally correct, but adds extra work not present in the JS.

FINDING: map watcher does not call setToDefaultPosition immediately (JS does, C++ defers)
  JS Source: src/js/components/map-viewer.js lines 143–150
  Details: In JS, the `map` watcher calls `this.clearTileState()` then `this.setToDefaultPosition()` (which internally calls `this.render()`). In C++ `renderWidget` at lines 1237–1243, when `state.prevMap != mapId` it clears tile state and sets `state.initialized = false` to defer the default position until the next frame (when viewport dimensions are available). This is a reasonable adaptation for ImGui, but `setToDefaultPosition` is deferred until `viewportWidth > 0.0f && viewportHeight > 0.0f` is true, so there is a one-frame delay before the default position is applied.

FINDING: The info panel renders as a horizontal row of same-line labels rather than distinct `<span>` elements with CSS spacing
  JS Source: src/js/components/map-viewer.js lines 1101–1113 (template)
  Details: The JS template uses a `<div class="info">` containing multiple `<span>` elements that are styled separately via CSS. The C++ version (lines 1267–1286) renders them all on one line with `ImGui::SameLine()`. This is layout-functionally correct (all labels present, same content), but the visual separation between spans differs from the CSS-styled version. Minor visual difference; acceptable given the Visual Fidelity rules.

FINDING: handleKeyPress only checks for uppercase 'D' but not lowercase 'd'
  JS Source: src/js/components/map-viewer.js lines 820–826
  Details: JS line 820: `else if (event.key === 'd' || event.key === 'D')`. The C++ version at line 897 checks only `ImGui::IsKeyPressed(ImGuiKey_D)`. ImGui's key press detection is typically case-insensitive for letter keys (it tracks the physical key, not the character). This should be functionally equivalent in practice, but it is worth noting since the JS explicitly handles both cases.

FINDING: handleKeyPress only checks for uppercase 'B' but not lowercase 'b'
  JS Source: src/js/components/map-viewer.js lines 826–839
  Details: JS line 826: `else if (event.key === 'b' || event.key === 'B')`. Same situation as 'D' above — C++ uses `ImGui::IsKeyPressed(ImGuiKey_B)` which covers the physical key regardless of shift state. Functionally equivalent but differs from explicit JS dual-case check.

FINDING: handleMouseOut sets hoverTile to -1 but JS sets it to null (semantic difference)
  JS Source: src/js/components/map-viewer.js lines 1067–1072
  Details: JS sets `this.hoverTile = null` (line 1071). C++ sets `state.hoverTile = -1` (line 1176), using -1 as the null sentinel. The render overlay check uses `state.hoverTile == index` (all indices are >= 0), so -1 is never matched — functionally equivalent to null.

FINDING: hoverTile initialized to -1 in C++ vs null in JS — minor but documented
  JS Source: src/js/components/map-viewer.js lines 57–63 (data())
  Details: JS data() initializes `hoverTile: null`. C++ MapViewerState uses `int hoverTile = -1`. This is a consistent and documented sentinel value, and is functionally correct.

FINDING: Missing `openMenu` method logic — JS openMenu toggles: `this.open = !this.open && !this.disabled`
  JS Source: src/js/components/map-viewer.js — NOT applicable; this finding is for menu-button.

AUDIT_TRACKER_CHECK: 113

---

## File: src/js/components/markdown-content.cpp (audit entry #114)

The markdown-content.cpp file does not exist — per CLAUDE.md it has been deliberately removed from the C++ build. The file is listed under "Removed Files" in CLAUDE.md. The `.js` counterpart exists at `src/js/components/markdown-content.js`. No `.cpp` or `.h` stub files exist for this module, which is correct per the removal policy. There is no documentation comment to evaluate (the file simply does not exist).

NO_ISSUES

AUDIT_TRACKER_CHECK: 114

---

## File: src/js/components/menu-button.cpp (audit entry #115)

FINDING: Missing `openMenu` method and its toggle logic
  JS Source: src/js/components/menu-button.js lines 37–40
  Details: The JS component has an `openMenu` method that toggles open state only when enabled: `this.open = !this.open && !this.disabled`. The C++ version directly calls `ImGui::OpenPopup()` on arrow click or dropdown button click (lines 141, 156), bypassing the toggle-and-disable guard. The JS logic prevents re-opening when `disabled` is true (even if the caller passes disabled=false via the prop), whereas C++ wraps everything in `if (disabled) ImGui::BeginDisabled()`, which prevents interaction at the ImGui level. Functionally equivalent in the common case, but the JS openMenu's `!this.open` toggle part is lost — clicking the arrow while the popup is open in JS would close it, whereas in C++ clicking again while the popup is open would call `ImGui::OpenPopup` again (which is a no-op if already open, so effectively the same).

FINDING: `select` method in JS closes the menu AND emits 'change'; C++ close is via ImGui::CloseCurrentPopup in the selectable handler
  JS Source: src/js/components/menu-button.js lines 27–30
  Details: JS `select(option)` sets `this.open = false`, stores the selected object, and emits `'change'`. In C++ (lines 183–185), `select()` is called and `ImGui::CloseCurrentPopup()` is called after — same net effect. The `onChange` callback corresponds to `$emit('change')`. This is functionally equivalent.

FINDING: `upward` prop not present in JS source — added in C++ without a JS counterpart
  JS Source: src/js/components/menu-button.js (entire file)
  Details: The JS component has `props: ['options', 'default', 'disabled', 'dropdown']` with no `upward` prop. The C++ `render` function takes a `bool upward` parameter (line 105) which controls whether the popup opens above or below the button. This is a C++ addition with no JS counterpart. The feature is useful for ImGui (popups that would be clipped at screen bottom), but it is a deviation from the JS component's interface.

FINDING: `drawArrowButton` uses raw ImDrawList calls for the arrow triangle and separator line
  JS Source: src/js/components/menu-button.js lines 75–82 (template: arrow div with CSS)
  Details: The JS uses a CSS-styled `<div class="arrow">` which renders as a downward-pointing triangle via CSS. The C++ version uses `ImDrawList::AddTriangleFilled` and `ImDrawList::AddLine` for the arrow and separator (lines 80–95). Per CLAUDE.md, raw ImDrawList calls should be avoided for things a native widget handles. ImGui's `ImGui::ArrowButton` or a simple `▼` text character could replace the custom triangle. Minor violation of the "no raw ImDrawList for native widget equivalents" convention, but the rendered result is visually equivalent.

FINDING: Separator lines between options in the popup use raw ImDrawList::AddLine
  JS Source: src/js/components/menu-button.js lines 78–80 (context-menu options)
  Details: The JS renders options as `<span>` elements inside a context-menu component that applies CSS separator styling. The C++ version (lines 187–194) draws separator lines between options using `drawList->AddLine(...)`. Per CLAUDE.md guidelines, `ImGui::Separator()` should be preferred over raw ImDrawList calls. The raw line draw is functionally correct but violates the convention.

NO MAJOR LOGIC ERRORS found beyond the above. The component is functionally complete and faithful to the JS original.

AUDIT_TRACKER_CHECK: 115

---

## File: src/js/components/model-viewer-gl.cpp (audit entry #116)

FINDING: JS `render()` method has a single `this.controls` reference; C++ splits into orbit_controls/char_controls
  JS Source: src/js/components/model-viewer-gl.js lines 211–366
  Details: The JS component stores one duck-typed `this.controls` that works for both `CameraControlsGL` and `CharacterCameraControlsGL`. The C++ version uses two separate typed pointers (`orbit_controls` and `char_controls`) and a `controls_update` callback. This is a deliberate and documented architectural difference. The comment at lines 182–186 of the header explains this. No functional regression, but callers using `context.controls` in JS need to use `context.controls_orbit` or `context.controls_character` in C++.

FINDING: `fit_camera_for_character` in JS takes a single `controls` parameter (duck-typed); C++ splits into two typed pointers
  JS Source: src/js/components/model-viewer-gl.js lines 186–205
  Details: JS `fit_camera_for_character(bounding_box, camera, controls)` calls `controls.update()` if controls is non-null. The C++ version takes two typed pointers (`orbit_controls` and `char_controls`) and updates only the non-null one. The comment at lines 203–205 explains this. Functionally equivalent, but any future caller expecting a single controls parameter will need updating.

FINDING: JS `render()` uses `performance.now() * 0.001` for time; C++ uses `ImGui::GetTime()`
  JS Source: src/js/components/model-viewer-gl.js line 215
  Details: JS gets time in seconds via `performance.now() * 0.001`. C++ uses `ImGui::GetTime()` (line 394). `ImGui::GetTime()` is the elapsed time since ImGui initialization (typically app start), not a wall-clock epoch. This is functionally equivalent for computing deltaTime.

FINDING: JS checks `this.lastTime === undefined` for first-frame detection; C++ uses `state.lastTime < 0`
  JS Source: src/js/components/model-viewer-gl.js lines 216–218
  Details: JS: `if (this.lastTime === undefined) this.lastTime = currentTime;`. C++: `if (state.lastTime < 0) state.lastTime = currentTime;`. State initializes lastTime to -1.0f (header line 229). Functionally equivalent.

FINDING: JS auto-rotation only calls setTransform via activeRenderer; C++ has a second branch via context.setActiveModelTransform
  JS Source: src/js/components/model-viewer-gl.js lines 239–247
  Details: JS: `if (rotation_speed !== 0 && activeRenderer && activeRenderer.setTransform && !this.use_character_controls)` — only uses activeRenderer. C++ has a second branch (lines 436–443): `else if (rotation_speed != 0 && !state.use_character_controls && context.setActiveModelTransform)` — this is a C++ addition with no JS counterpart. The second branch handles the case where activeRenderer is null but a generic transform setter is available. This is an extension that is not in the JS.

FINDING: JS `recreate_controls` disposes a single `this.controls` then creates a new one; C++ disposes both orbit_controls and char_controls separately
  JS Source: src/js/components/model-viewer-gl.js lines 369–395
  Details: JS disposes `this.controls` (one reference). C++ disposes `state.orbit_controls` and `state.char_controls` separately (lines 608–615). Functionally equivalent (only one is non-null at a time in practice), but if both were somehow non-null both would be disposed.

FINDING: JS watchers use `core.view.$watch()` (Vue reactive); C++ uses polling via BoolWatcher
  JS Source: src/js/components/model-viewer-gl.js lines 468–474
  Details: JS: `this.watchers.push(core.view.$watch('config.chrUse3DCamera', () => ...))` — reactive. C++ polls each bool each frame via `poll_watchers` (lines 700–709), comparing previous vs current value. Functionally equivalent for callbacks triggered by value changes, but C++ has one-frame latency compared to the reactive JS approach.

FINDING: `dispose()` does not call `state.grid_renderer->dispose()` after reset in some paths
  JS Source: src/js/components/model-viewer-gl.js lines 501–513
  Details: JS `beforeUnmount` calls `this.grid_renderer.dispose()` (line 504) and `this.shadow_renderer?.dispose()` (line 505). C++ `dispose()` at lines 796–804 calls `state.grid_renderer->dispose()` and then `state.grid_renderer.reset()`, then similarly for shadow_renderer. This is correct. No issue here.

FINDING: JS `beforeUnmount` removes the resize event listener; C++ has no equivalent unregistration
  JS Source: src/js/components/model-viewer-gl.js lines 501–513
  Details: JS line 507: `window.removeEventListener('resize', this.onResize)`. In C++, the resize is handled by ImGui layout polling each frame (renderWidget lines 843–856), so there is no event listener to unregister. This is a valid ImGui adaptation — no issue.

FINDING: `context.controls` JS reference is replaced by two typed C++ pointers but the JS API contract is not preserved for external callers
  JS Source: src/js/components/model-viewer-gl.js line 395
  Details: JS sets `this.context.controls = this.controls;` after recreate_controls. External JS code can call `this.context.controls.someMethod()`. In C++, callers must know to use `context.controls_orbit` vs `context.controls_character`. This is a breaking interface change relative to JS, though documented in the header.

FINDING: `render()` calls `requestAnimationFrame` recursively (JS); C++ calls `renderWidget` per ImGui frame
  JS Source: src/js/components/model-viewer-gl.js line 366
  Details: JS schedules the next frame via `requestAnimationFrame(() => this.render())`. C++ calls `renderWidget` each ImGui frame from the parent tab module. Functionally equivalent — both produce one render per display frame.

NO additional logic deviations found. The component is comprehensively converted.

AUDIT_TRACKER_CHECK: 116

---

## File: src/js/components/resize-layer.cpp (audit entry #117)

FINDING: JS emits `clientWidth` (integer, always); C++ callback receives float from ImGui::GetContentRegionAvail().x
  JS Source: src/js/components/resize-layer.js lines 13–15
  Details: JS: `this.observer = new ResizeObserver(() => this.$emit('resize', this.$el.clientWidth))`. `clientWidth` is always an integer (pixels, rounded down). The C++ version passes `currentWidth` (float, from `GetContentRegionAvail().x`) to the callback (line 44). The comparison uses integer truncation (`currentWidthInt != prevWidthInt`) to debounce, but the callback itself receives the float. If the JS contract expects an integer, callers receiving a float may round differently.

FINDING: C++ implementation uses BeginGroup/EndGroup; JS uses a plain `<div>` wrapper
  JS Source: src/js/components/resize-layer.js line 25 (template)
  Details: JS template: `<div><slot></slot></div>`. C++ uses `ImGui::BeginGroup()` / `ImGui::EndGroup()` (lines 32 and 51). BeginGroup/EndGroup affects ImGui layout (groups the content for layout operations), whereas a bare `<div>` in HTML is just a block container. The visual effect is equivalent for this use case (content wrapper), but BeginGroup may affect cursor positioning in edge cases.

FINDING: JS uses ResizeObserver on the element for width changes; C++ polls every frame
  JS Source: src/js/components/resize-layer.js lines 12–15
  Details: JS fires the callback only when the element's width actually changes (event-driven via ResizeObserver). C++ calls `GetContentRegionAvail().x` every frame and fires if the int-truncated value changed (lines 40–44). This is functionally equivalent but has slightly different timing — C++ fires on the next rendered frame after the resize rather than asynchronously (ResizeObserver fires asynchronously per the spec).

NO MAJOR ISSUES. The component is minimal and correctly ported.

AUDIT_TRACKER_CHECK: 117

---

## File: src/js/components/slider.cpp (audit entry #118)

FINDING: The entire custom slider interaction logic is replaced by ImGui::SliderFloat — not a line-by-line port
  JS Source: src/js/components/slider.js lines 41–99
  Details: The JS slider is a fully custom widget with mousedown/mousemove/mouseup document listeners for dragging, a click handler for jump-to-position, and a custom visual template with a fill bar and a handle element. The C++ version replaces all of this with a single `ImGui::SliderFloat` call (lines 27–30). While ImGui::SliderFloat provides equivalent user interaction (drag, click-to-set), the visual appearance (custom fill div + handle div) is completely different from the ImGui default slider appearance. Per CLAUDE.md Visual Fidelity rules, the layout must match but exact styling is not required — so this replacement is acceptable. However, the JS slider renders as a custom-styled track+handle widget, while ImGui::SliderFloat renders as a standard ImGui slider. This is a known and acceptable deviation given the visual fidelity rules.

FINDING: JS slider emits 'update:modelValue'; C++ calls onChange callback — interface difference
  JS Source: src/js/components/slider.js lines 46–48
  Details: JS: `this.$emit('update:modelValue', Math.min(1, Math.max(0, value)))`. The JS clamps the value to [0, 1] before emitting. C++ calls `onChange(v)` with the raw ImGui slider value. However, since ImGui::SliderFloat is called with min=0.0f and max=1.0f, the value is already clamped by ImGui before onChange is called. Functionally equivalent.

FINDING: JS explicitly registers and unregisters global document mousemove/mouseup listeners; C++ has no equivalent
  JS Source: src/js/components/slider.js lines 24–38
  Details: JS registers document-level mousemove/mouseup to handle drag outside the slider element. C++ relies on ImGui::SliderFloat's built-in drag handling, which uses ImGui's internal mouse capture mechanism (equivalent functionality). This is a valid ImGui adaptation.

FINDING: SliderState parameter in C++ render is unused (commented out with [[maybe_unused]])
  JS Source: src/js/components/slider.js (entire file)
  Details: C++ `render` function signature takes `SliderState& /*state*/` (line 22) but never uses it. The JS component has reactive `isScrolling` state; in C++ this state is managed internally by ImGui::SliderFloat. The SliderState struct may be vestigial if it exists in the header. This is a minor code quality issue but not a logic error.

AUDIT_TRACKER_CHECK: 118

---
