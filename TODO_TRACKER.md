# TODO Tracker

> **Progress: 0/50 verified (0%)** — ✅ = Verified, ⬜ = Pending

- [ ] 1. [app.cpp] Drag-enter / drag-leave handlers not implemented; fileDropPrompt overlay never appears during drag-over.
  - **JS Source**: `src/app.js` lines 589–624, 649–657
  - **Status**: Pending
  - **Details**: JS installs `window.ondragenter` and `window.ondragleave` to track a `dropStack` counter and set `core.view.fileDropPrompt` to a handler-supplied message (e.g. "Export N files as ...") while the user hovers files over the window. The fileDropPrompt overlay is rendered in C++ (renderAppShell lines 1055–1108) but the C++ port only registers `glfwSetDropCallback` (the "drop" event), so `fileDropPrompt` is set to nullptr on drop and never set during hover. Comment at app.cpp:2425–2437 acknowledges this as a GLFW API limitation; restoration would require Win32 IDropTarget / X11 XDnD. Net effect: users get no visual feedback while dragging files.
- [ ] 2. [app.cpp] Auto-updater branch is permanently disabled instead of being conditional on BUILD_RELEASE && !DISABLE_AUTO_UPDATE.
  - **JS Source**: `src/app.js` lines 688–705
  - **Status**: Pending
  - **Details**: JS runs `updater.checkForUpdates()`, applying an update or hiding the loading screen + setting source_select active + checking blender on the result. C++ (app.cpp:2455–2486) has the entire `if (BUILD_RELEASE && !DISABLE_AUTO_UPDATE)` block commented out, then unconditionally calls `tab_blender::checkLocalVersion()` and `modules::setActive("source_select")` regardless of build flavour. Documented as deviation in comments, but functionally release builds never check for updates and never display the "Checking for updates..." loading screen.
- [ ] 3. [app.cpp] activeModule watcher uses indiscriminate resetAll() instead of the JS per-entry true→false / else→null logic.
  - **JS Source**: `src/app.js` lines 556–564
  - **Status**: Pending
  - **Details**: JS iterates contextMenus entries: `value === true` becomes `false`, anything else (and not `false`) becomes `null`. C++ checkWatchers (app.cpp:2058–2063) calls `core::view->contextMenus.resetAll()` which does not preserve the JS's three-state distinction. Whether this changes behaviour depends on `ContextMenus::resetAll()`, but the literal JS semantics (preserving `false` while nulling out non-true non-false values) is not preserved.
- [ ] 4. [app.cpp] reloadStylesheet does not actually reload styling — it re-applies the static ImGui dark theme.
  - **JS Source**: `src/app.js` lines 160–164
  - **Status**: Pending
  - **Details**: JS rewrites every `<link rel=stylesheet>` href with a cache-busting `?v=Date.now()` query so updated CSS files are picked up without restarting. C++ (app.cpp:1344–1346) just calls `ImGui::StyleColorsDark()`, which is a no-op for theming changes since C++ has no equivalent reloadable stylesheet. As a result, the "Reload Styling" context menu option (registered at 2386) is effectively inert. Per CLAUDE.md visual-fidelity rules this is acceptable, but it deviates from the JS dev-tool intent.
- [ ] 5. [app.cpp] No global click delegate for [data-external] and [data-kb-link] elements.
  - **JS Source**: `src/app.js` lines 116–131
  - **Status**: Pending
  - **Details**: JS installs a single document-level `click` listener that intercepts any element with `data-kb-link` (routes to `tab_help.open_article`) or `data-external` (routes to `ExternalLinks.open`). C++ has no analogous shell-wide handler — every consumer must explicitly call `ExternalLinks::open` itself. The footer in renderAppShell does this manually, but any module that emits a `data-external` attribute through templated rendering (e.g. for Discord/GitHub/Patreon cards in tab_home, the WhatsNew HTML panel) will not auto-resolve those links. tab_help is removed per CLAUDE.md so the kb-link path is N/A, but the data-external delegate is still a missing feature that other modules rely on.
- [ ] 6. [blob.cpp] `stringEncode` does not perform UTF-16 → UTF-8 conversion or surrogate-pair handling.
  - **JS Source**: `src/js/blob.js` lines 42–95
  - **Status**: Pending
  - **Details**: The JS `stringEncode` implements a full UTF-16 → UTF-8 encoder: it walks `String.charCodeAt`, recombines high/low surrogate pairs (0xD800–0xDBFF + 0xDC00 follower) into supplementary code points, emits 1/2/3/4-byte UTF-8 sequences, dynamically resizes the target buffer, and skips lone high surrogates (`continue`). The C++ `stringEncode` (blob.cpp 68–72) just memcpys the input `std::string` bytes into a `vector<uint8_t>`, assuming the input is already valid UTF-8. For UTF-8 input the byte output is identical, but any callers passing non-UTF-8 / lone surrogates / UTF-16 encoded text will get garbage where JS would produce well-formed UTF-8 (or skip lone surrogates). The deviation is acknowledged by an in-source comment, but no replacement implementation is provided.
- [ ] 7. [blob.cpp] `stringDecode` emits UTF-8 instead of UTF-16 code units.
  - **JS Source**: `src/js/blob.js` lines 97–167
  - **Status**: Pending
  - **Details**: The JS `stringDecode` decodes UTF-8 bytes, splits supplementary code points into UTF-16 surrogate pairs (lines 149–153), pushes UTF-16 code units into `res`, and assembles a JS string via `String.fromCharCode` chunks of 0x1000. The C++ `stringDecode` (blob.cpp 87–185) decodes the same bytes but re-encodes valid code points back to UTF-8 directly, never producing surrogate pairs. While the underlying Unicode content is preserved, the output type and byte values for non-BMP code points differ between the two implementations. Comment acknowledges this; flagged for audit completeness.
- [ ] 8. [blob.cpp] `slice()` end-argument semantics differ from JS falsy-zero behavior.
  - **JS Source**: `src/js/blob.js` lines 262–265
  - **Status**: Pending
  - **Details**: JS `slice(start, end, type)` evaluates `end || this._buffer.length`, so `slice(x, 0)` returns the full buffer (because `0` is falsy). The C++ implementation (blob.cpp 305–322) only treats `nullopt` as "use full length" — an explicit `end == 0` is honored as zero, making `slice(0, 0)` an empty blob in C++ but a full-length blob in JS. The deviation is intentional and documented, but it is a behavioral difference that any caller relying on JS falsy semantics would observe.
- [ ] 9. [blob.cpp] `BlobPolyfill::stream()` is synchronous and lacks ReadableStream backpressure.
  - **JS Source**: `src/js/blob.js` lines 271–288
  - **Status**: Pending
  - **Details**: JS returns a `ReadableStream` whose `pull(controller)` chains `arrayBuffer().then(...)`, advances `position`, enqueues a `Uint8Array`, and calls `controller.close()` when `position == blob.size`. Control flow is consumer-driven via the streams spec backpressure protocol. C++ returns a `BlobReadableStream` whose `pull()` synchronously slices 512KB chunks and a callback overload that drains the entire blob in a blocking loop (blob.cpp 340–348, 361–376). No async scheduling, no `controller.enqueue/close` parity, and any callers expecting Promise/async semantics are silently changed. Documented in source comment.
- [ ] 10. [blob.cpp] `URLPolyfill` adds extra members (`createObjectURL(span)`, `resolveObjectURL`, internal map) not present in JS.
  - **JS Source**: `src/js/blob.js` lines 293–307
  - **Status**: Pending
  - **Details**: The JS class only exposes `createObjectURL(blob)` and `revokeObjectURL(url)`. The C++ port adds a second overload `createObjectURL(std::span<const uint8_t> bytes, std::string_view type)` (blob.cpp 396–405) that mints `blob:wow.export.cpp/<counter>` URLs and stores them in a global mutex-protected map (`g_objectUrls`, blob.cpp 203–210), plus a brand-new `resolveObjectURL(url)` (blob.cpp 407–413) declared in blob.h line 178. Additionally, `revokeObjectURL` was retasked from "call native URL.revokeObjectURL for non-data URLs" to "erase from the internal map for non-data URLs" (blob.cpp 430–435). These are EXTRAs introducing behavior with no JS counterpart and changing the documented JS branch.
- [ ] 11. [buffer.cpp] readBuffer(length, bool) reinterprets the second positional argument as `inflate` rather than JS's `wrap`.
  - **JS Source**: `src/js/buffer.js` lines 531–543
  - **Status**: Pending
  - **Details**: JS signature is `readBuffer(length=remaining, wrap=true, inflate=false)`. The 2-arg JS call `readBuffer(N, false)` returns a raw `Buffer` (wrap=false). The C++ 2-arg overload `readBuffer(size_t length, bool inflate = false)` (buffer.h line 309, buffer.cpp lines 815–825) interprets the second argument as `inflate`, so a JS-style call `readBuffer(N, false)` in C++ returns a wrapped `BufferWrapper` and never disables wrapping. Existing call sites compensate (e.g. `LoaderGenerics.cpp` line 31 captures into a `BufferWrapper` and calls `.raw()`), but the API contract differs from JS and any future port that copies the JS pattern verbatim will silently get wrap=true. The 3-arg `readBuffer(length, wrap, inflate)` variant exists, but the 2-arg shortcut diverges from JS.
- [ ] 12. [buffer.cpp] alloc()/setCapacity() ignore the `secure` flag and always zero the buffer.
  - **JS Source**: `src/js/buffer.js` lines 54–56, 1021–1029
  - **Status**: Pending
  - **Details**: JS uses `Buffer.allocUnsafe(length)` when `secure=false` (uninitialised memory) and `Buffer.alloc(length)` (zeroed) when `secure=true`. C++ `BufferWrapper::alloc` (buffer.cpp line 387) always uses `std::vector<uint8_t>(length, 0)` and `setCapacity` (buffer.cpp line 1128) always uses `std::vector<uint8_t>(capacity, 0)`. The `secure` parameter is accepted but unused. While safer, this is an observable deviation: callers that allocate large buffers for sequential overwrite incur an extra zero-fill cost the JS version does not, and any (broken) JS callers that read pre-fill bytes would see different values.
- [ ] 13. [buffer.cpp] getDataURL passes "application/octet-stream" but JS Blob has no explicit type (defaults to empty string).
  - **JS Source**: `src/js/buffer.js` lines 989–995
  - **Status**: Pending
  - **Details**: JS `new Blob([this.internalArrayBuffer])` constructs a Blob with no type argument; per spec the Blob's `type` is `""`. The C++ `getDataURL` (buffer.cpp line 1078) calls `URLPolyfill::createObjectURL(_buf, "application/octet-stream")`. Since `URLPolyfill::createObjectURL` produces a `data:<type>;base64,…` URL (per blob.h docstring lines 168–175), the resulting URL prefix differs from what JS would produce (browsers also produce a `blob:` URL not a `data:` URL, but that polyfill choice is outside this file). Pass an empty string to `createObjectURL` (or its no-type overload) to match JS Blob default-type semantics.
- [ ] 14. [buffer.cpp] fromCanvas only supports image/webp and image/png; JS supports any browser-supported mimeType (notably image/jpeg).
  - **JS Source**: `src/js/buffer.js` lines 89–107
  - **Status**: Pending
  - **Details**: The JS implementation defers to `canvas.convertToBlob({type, quality})` / `canvas.toBlob(cb, type, quality)`, which the browser implements for any supported mime type (typically `image/png`, `image/jpeg`, `image/webp`). The C++ implementation (buffer.cpp lines 416–459) explicitly handles only `image/webp` (lossless and lossy) and `image/png`; any other mimeType throws `runtime_error("fromCanvas: unsupported mimeType …")`. Specifically, `image/jpeg` is unsupported in C++. If any caller uses JPEG output, the C++ port will throw where JS would succeed.
- [ ] 15. [buffer.cpp] readString 'ascii' encoding is treated as UTF-8 byte copy; Node Buffer.toString('ascii') masks each byte with 0x7F.
  - **JS Source**: `src/js/buffer.js` lines 551–561
  - **Status**: Pending
  - **Details**: Node's `Buffer.toString('ascii')` documents that it strips the high bit of each byte (i.e. bytes are masked with 0x7F before being output). The C++ `readString` (buffer.cpp lines 849–871) groups `'ascii'` together with `'utf8'`/`'latin1'`/`'binary'` and just does a raw byte copy via `str.assign(reinterpret_cast<const char*>(ptr), length)`. For bytes ≥ 0x80 the resulting string differs (JS would yield bytes with the high bit cleared; C++ keeps them as-is and treats them as UTF-8 lead bytes). The `latin1`/`binary` grouping is correct (those preserve all 256 byte values); only `ascii` should additionally strip the high bit.
- [ ] 16. [config.cpp] Missing `core.view.$watch('config', () => save(), { deep: true })` auto-save mechanism.
  - **JS Source**: `src/js/config.js` line 60
  - **Status**: Pending
  - **Details**: JS `load()` registers a deep Vue `$watch` on `core.view.config` that calls `save()` automatically on any mutation. C++ `load()` (config.cpp lines 109–111) only includes a comment acknowledging the deviation and does not install any equivalent reactive watcher. As a result, any mutation to `core::view->config` from elsewhere in the app will not be persisted unless that call site manually invokes `config::save()`. JS guarantees persistence; C++ requires opt-in at every call site, so config changes can be silently lost. This is a documented but unfixed functional deviation.
- [ ] 17. [config.cpp] `save()` exported as public API in `config.h`, but JS keeps it private.
  - **JS Source**: `src/js/config.js` line 118
  - **Status**: Pending
  - **Details**: JS `module.exports` deliberately omits `save` — it is module-private and only called from the internal `$watch` callback installed in `load()`. C++ declares `void save();` in `config.h` (line 38) inside the `config` namespace, making it public. This is an EXTRA introduced as a workaround for the missing `$watch` mechanism (callers now must call `config::save()` directly). It widens the public API beyond what JS exposes; if the `$watch` equivalent is added, `save` should be moved back to internal linkage in `config.cpp` and removed from the header.
- [ ] 18. [constants.h] VERSION hardcoded to "0.1.0" instead of reading from the application manifest like JS does.
  - **JS Source**: `src/js/constants.js` line 52
  - **Status**: Pending
  - **Details**: JS exports `VERSION: nw.App.manifest.version`, sourcing the version dynamically from the NW.js package manifest. The C++ port (`constants.h:39`) uses `inline constexpr std::string_view VERSION = "0.1.0";`. The deviation is documented in a comment, but it changes behaviour: USER_AGENT becomes "wow.export (0.1.0)" instead of the real release version, and any update-check logic comparing this VERSION against remote/manifest values will be wrong unless manually bumped per release. There is no build-system substitution wired up. Should at minimum be sourced from a build-time generated header or CMake variable to track the real product version.
- [ ] 19. [constants.cpp] CONFIG.DEFAULT_PATH resolves via the CMake source directory rather than the executable's install path.
  - **JS Source**: `src/js/constants.js` line 101
  - **Status**: Pending
  - **Details**: JS sets `CONFIG.DEFAULT_PATH = path.join(INSTALL_PATH, 'src', 'default_config.jsonc')` where `INSTALL_PATH = path.dirname(process.execPath)` — i.e. resources sit next to the deployed executable. C++ (`constants.cpp:140` and `:174`) sets `s_src_dir = fs::path(WOW_EXPORT_SOURCE_DIR) / "src"` (CMake's source dir), then `s_config_default_path = s_src_dir / "default_config.jsonc"`. This works only when the binary is run from the build tree against the original repo path baked into the binary at compile time; an installed/redistributed build will look at a path that doesn't exist on the user's machine. JS resolves this correctly at runtime relative to `process.execPath`. SHADER_PATH (`constants.cpp:155`) and any other consumer of `s_src_dir` inherits the same defect.
- [ ] 20. [core.h] Missing AppState fields chrItemPickerSlot and chrItemPickerFilter from makeNewView().
  - **JS Source**: `src/js/core.js` lines 269–270
  - **Status**: Pending
  - **Details**: JS makeNewView() initializes `chrItemPickerSlot: null` (slot ID for item picker modal — null means hidden) and `chrItemPickerFilter: null` (slot filter name for the modal). Both fields are read/written across `tab_characters.js` (lines 2306, 2535–2541) and bound into the Vue template that conditionally renders ItemPickerModal. The C++ AppState in `core.h` does not declare either field. The C++ port works around this by using a separate `item_picker_modal::open()/close()` namespace (`src/js/components/item-picker-modal.h`) that owns its own state, but the AppState fields themselves are absent — any code path that inspects `core::view->chrItemPickerSlot` (as the JS Vue template does to conditionally mount the modal) cannot be ported faithfully. Per CLAUDE.md the C++ port must be 100% functionally identical and every makeNewView() field must be present on AppState; these two are missing.
- [ ] 21. [external-links.cpp] renderLink() uses raw ImDrawList::AddText / AddLine for the hover effect, violating the CLAUDE.md ImGui rendering rule.
  - **JS Source**: `src/js/external-links.js` (no JS counterpart — JS uses a global DOM `data-external` click handler in `app.js` lines ~115–131)
  - **Status**: Pending
  - **Details**: The C++ port adds a helper `renderLink()` (cpp lines 71–89, header line 73) to replace the missing DOM click handler. Inside, on hover it calls `ImDrawList::AddText` and `ImDrawList::AddLine` to redraw the label in white and draw an underline. CLAUDE.md's "ImGui rendering" convention explicitly reserves `ImDrawList` for "image rotation, multi-colour gradient fills, and custom OpenGL overlays" and forbids it "for anything a native widget handles." A hover-styled link can be done with native ImGui (e.g. `PushStyleColor(ImGuiCol_Text, ...)` around `TextUnformatted`). The JS original simply calls `nw.Shell.openExternal` with no custom hover styling — there is no need to draw a hover underline at all to be JS-faithful. Functionality (clicking calls `open(link)`) is correct; the deviation is purely in the hover visual treatment.
- [ ] 22. [file-writer.cpp] Constructor `encoding` parameter is silently ignored (`std::string_view /*encoding*/`).
  - **JS Source**: `src/js/file-writer.js` lines 14–24
  - **Status**: Pending
  - **Details**: JS passes `encoding` through to `fs.createWriteStream(file, { flags: 'w', encoding })`, which controls how JS strings are encoded to bytes (utf8, utf16le, ascii, latin1, etc.). The C++ port discards this parameter and always writes raw `char` bytes via `std::ofstream`, so any caller that relies on a non-utf8 encoding (e.g. UTF-16LE for a tool that consumes BOM'd output) will receive wrong bytes. The encoding hint must actually be honoured (e.g. by transcoding `line` inside `writeLine` based on the stored encoding) or the C++ API should at minimum reject non-utf8 encodings rather than silently write raw bytes.
- [ ] 23. [file-writer.cpp] `writeLine` is fully synchronous; backpressure (`blocked`/`resolver`/`_drain`) is not implemented.
  - **JS Source**: `src/js/file-writer.js` lines 26–48
  - **Status**: Pending
  - **Details**: JS `writeLine` is `async`: when a previous write returned `false` (Node stream backpressure), the next call awaits a Promise that resolves when the underlying stream emits `'drain'`. The C++ port has no `blocked`/`resolver` state, no `_drain` helper, and `writeLine` returns `void` rather than a future, so it cannot suspend the caller. Documented as a deviation in the header but still a behavioural divergence — JS callers that `await writeLine` in tight loops yield to the event loop and let other tasks run; the C++ equivalent monopolises the calling thread until the OS buffer accepts the write. To preserve fidelity, `writeLine` should return a `std::future<void>` (or equivalent) that completes immediately when the write succeeds and only suspends when the stream is saturated.
- [ ] 24. [file-writer.cpp] EISDIR retry path uses `is_directory` heuristic instead of error-code dispatch.
  - **JS Source**: `src/js/file-writer.js` lines 15–24
  - **Status**: Pending
  - **Details**: JS catches the open error and branches strictly on `e.code === 'EISDIR'` — only directories trigger the recursive-remove + retry; everything else rethrows. C++ instead opens, checks `!stream.is_open()`, then asks `std::filesystem::is_directory(file)`. This works for the common case but diverges in edge cases: (a) if `is_directory` returns false for a non-EISDIR open failure (permissions, sharing violation, ENOENT on a parent), C++ throws a generic `std::runtime_error("failed to open file for writing: …")` rather than letting the underlying error propagate with its original code/message; (b) if the path becomes a directory between two checks, behaviour can race. The C++ thrown error also loses the original errno information that JS preserves (`e.code`, `e.message`). Consider opening with `errno` capture (or `std::error_code` overload) and dispatching on `std::errc::is_a_directory` to mirror JS exactly.
- [ ] 25. [generics.cpp] downloadFile() skips chmod entirely on Windows.
  - **JS Source**: `src/js/generics.js` lines 218–252
  - **Status**: Pending
  - **Details**: JS calls `fsp.chmod(out, mode)` unconditionally on every platform. On Windows, Node's fs.chmod still maps the user-write bit to the file's read-only attribute (e.g. mode `0o400` makes the file read-only). The C++ implementation guards the chmod call with `#ifndef _WIN32` (lines 622–624 of generics.cpp), so on Windows the `mode` argument is silently ignored after being masked at line 607. Any caller relying on `0o400`/`0o444` to set a read-only file on Windows will see different behaviour between JS and C++.
- [ ] 26. [generics.cpp] filesize() with NaN input returns the literal string "NaN" instead of the original input.
  - **JS Source**: `src/js/generics.js` lines 281–283
  - **Status**: Pending
  - **Details**: JS returns the original input value when `isNaN(input)` is true (e.g. `filesize("foo")` returns `"foo"`, `filesize(NaN)` returns the NaN value itself). C++ (lines 670–672 of generics.cpp) returns the literal string `"NaN"`. Any UI code that pipes an unparsable value through `filesize` will display `"NaN"` in C++ vs the original token in JS.
- [ ] 27. [generics.cpp] getFileHash() supports only "hex" and "base64" encodings; throws on any other value.
  - **JS Source**: `src/js/generics.js` lines 332–340
  - **Status**: Pending
  - **Details**: The JS implementation forwards `encoding` directly to `crypto.Hash.digest()`, which accepts every Node BufferEncoding value plus undefined (returns a Buffer). The C++ port (lines 327–341 of generics.cpp / `computeFileHash` helper) hard-codes a switch on `"hex"` and `"base64"` only and throws for anything else. The current call sites only pass `"hex"`, but the public API surface is a behavioural deviation that will silently regress if a future caller uses any other encoding.
- [ ] 28. [generics.cpp] requestData() 60-second timeout is split per-phase instead of an overall request timeout.
  - **JS Source**: `src/js/generics.js` lines 197–200
  - **Status**: Pending
  - **Details**: JS arms a single 60-second `req.setTimeout` that, on any 60s of inactivity, destroys the request and rejects with `'Request timeout after 60s'`. The C++ port (`doHttpGetRaw`, lines 213–222 of generics.cpp) sets `set_connection_timeout(60)` AND `set_read_timeout(60)` on the httplib client. These are per-phase: a request can spend 60s connecting and an additional 60s on each read without tripping the timeout, so the upper bound is significantly higher than 60s end-to-end. Also, on timeout cpp-httplib returns a generic error rather than `'Request timeout after 60s'`. The file's own comment on line 169–171 acknowledges this and points at TODO 51, so the deviation is known but not yet rectified.
- [ ] 29. [gpu-info.cpp] format_extensions strips 7 chars instead of 6 when removing the "WEBGL_" prefix from depth extensions.
  - **JS Source**: `src/js/gpu-info.js` line 250
  - **Status**: Pending
  - **Details**: JS line 250 does `ext.replace('WEBGL_', '')` for extensions containing `"depth"`, removing the 6-character prefix `WEBGL_`. The C++ port at `src/js/gpu-info.cpp` line 432 calls `name = name.substr(7)`, stripping 7 characters instead of 6. For example, the WebGL extension `WEBGL_depth_texture` becomes `depth_texture` in JS but `epth_texture` in the C++ output. The matching `EXT_` branch on line 434 correctly uses `substr(4)` (since `EXT_` is 4 chars), and the compressed/float categories use the correct lengths (25, 24, 12, 19, 17), so the issue is confined to the `WEBGL_` case in the depth category at gpu-info.cpp:432.
- [ ] 30. [icon-render.cpp] processQueue is fully synchronous and recursive instead of async/promise-chained — blocks the calling thread.
  - **JS Source**: `src/js/icon-render.js` lines 48–65
  - **Status**: Pending
  - **Details**: JS processQueue pops one entry, awaits `core.view.casc.getFile()` asynchronously, then calls `processQueue()` in `.finally()` so control returns to the event loop between every icon load, keeping the UI responsive. C++ processQueue at `src/js/icon-render.cpp` lines 252–289 calls `getVirtualFileByID` synchronously, decodes the BLP, uploads a GL texture, and recurses on the same stack via the unconditional call at line 288. With `QUEUE_LIMIT=20` up to 20 sequential synchronous CASC reads + BLP decodes + glTexImage2D calls happen on the calling thread before `loadIcon()` returns. The deviation is documented in the comment at lines 234–251 but is still a real behavioural divergence — large icon list refreshes can stall the UI thread, whereas the JS version yielded between each load. To match JS semantics this should be dispatched onto a worker thread (e.g. `std::async` or a coroutine) with the GL upload posted back to the render thread.
- [ ] 31. [icon-render.cpp] queueItem fallback path on expired weak_ptr has no JS counterpart.
  - **JS Source**: `src/js/icon-render.js` lines 77–91
  - **Status**: Pending
  - **Details**: JS queueItem stores entries by strong reference: `_queue.push({ fileDataID, rule })`. When the queue overflows it shifts the front entry and unconditionally calls `removeRule(removed.rule)` — the rule reference is always live. C++ stores `std::weak_ptr<IconRule>` in QueueEntry (`src/js/icon-render.cpp` line 167) and at lines 306–312 adds a fallback that re-resolves the rule via `_rulesByFileDataID` when `removed.rule.lock()` returns null. This branch only exists because of the weak_ptr design choice and has no analogue in JS; it does not change externally observable behaviour for the normal case but is dead/extra logic that complicates the port and could mask bugs where a rule pointer is unexpectedly released. Consider using `shared_ptr` in QueueEntry to mirror JS's strong-reference semantics directly.
- [ ] 32. [log.cpp] drainPool scheduling uses an ad-hoc drainPending flag piggybacked on write() instead of event-driven nextTick.
  - **JS Source**: `src/js/log.js` lines 47–48, 112
  - **Status**: Pending
  - **Details**: JS schedules `drainPool()` via the stream `'drain'` event listener (line 112) and recursive `process.nextTick(drainPool)` (line 48), so draining proceeds asynchronously and independently of new writes. C++ (`src/js/log.cpp` line 92, 123–124, 161–164) sets a `drainPending` flag and only drains when the next `write()` call happens. If no further `write()` occurs after a clog, pooled entries remain in memory indefinitely until shutdown `flush()`. The deviation is documented but is a real functional change — drain in JS is autonomous; in C++ it is piggybacked on the next write call.
- [ ] 33. [log.cpp] Synchronous std::ofstream has no backpressure, so the isClogged/pool path is effectively unreachable.
  - **JS Source**: `src/js/log.js` lines 81–90
  - **Status**: Pending
  - **Details**: JS `stream.write()` returns false when Node's internal buffer is full (true backpressure), causing the pooling branch (lines 83–89) to be exercised under load. In C++ (`src/js/log.cpp` lines 166–173) `isClogged` is only set when `stream.fail()` is true — i.e. a real I/O error occurred. `std::ofstream` has no backpressure semantics, so the pool / `MAX_LOG_POOL` / overflow-warning logic at lines 177–181 is effectively dead code under normal operation, whereas in JS it is the primary path for high-volume logging. The asynchronous-buffering behaviour of the JS module is not preserved.
- [ ] 34. [log.cpp] getErrorDump file-open error message differs from the JS exception text.
  - **JS Source**: `src/js/log.js` lines 102–108
  - **Status**: Pending
  - **Details**: JS calls `await fs.promises.readFile` and any error (ENOENT, EACCES, etc.) produces `'Unable to obtain runtime log: ' + e.message` where `e.message` is the Node-formatted error string (e.g. `'ENOENT: no such file...'`). C++ (`src/js/log.cpp` lines 264–266) tests the stream and returns the literal `'Unable to obtain runtime log: file could not be opened'` with no errno detail. The catch block at lines 271–273 only fires for `std::exception` during streambuf iteration. The diagnostic surfaced for a missing log file therefore differs from JS.
- [ ] 35. [log.cpp] Console mirror gated on NDEBUG instead of !BUILD_RELEASE — RelWithDebInfo loses stdout output.
  - **JS Source**: `src/js/log.js` lines 93–94
  - **Status**: Pending
  - **Details**: JS mirrors every line to `console.log` when `!BUILD_RELEASE` is true (lines 93–94); `BUILD_RELEASE` is a bundler-defined flag set only for production builds. C++ (`src/js/log.cpp` lines 184–187) uses `#ifndef NDEBUG`. Because `NDEBUG` is defined for both Release and RelWithDebInfo CMake configurations, the C++ port silences the stdout mirror in RelWithDebInfo, whereas the JS app would still print there. The `BUILD_RELEASE → NDEBUG` mapping is too coarse to preserve identical behaviour.
- [ ] 36. [log.cpp] write() invokes drainPool before sending the new line, creating ordering differences vs JS atomic pool/write.
  - **JS Source**: `src/js/log.js` lines 78–95
  - **Status**: Pending
  - **Details**: JS `write()` (lines 78–95) appends a single line and either writes it directly or pools it; drain runs out-of-band. C++ `write()` (`src/js/log.cpp` lines 161–164) drains the pool before sending the freshly built `line` string at line 157. When `drainPool` sets `isClogged=true` mid-drain, the just-built line is then pooled at lines 177–180 behind already-drained entries — semantics differ from JS where pooling decisions are based solely on `isClogged` at write time, with no intervening drain attempt that could change the flag.
- [ ] 37. [log.cpp] timeEnd variadic forwarding does not preserve JS util.format trailing-arg substitution.
  - **JS Source**: `src/js/log.js` lines 64–66
  - **Status**: Pending
  - **Details**: JS `timeEnd(label, ...params)` calls `write(label + ' (%dms)', ...params, elapsed)` (line 65), so `util.format` substitutes `...params` followed by `elapsed` into `label`'s format specifiers. C++ (`src/js/log.h` lines 64–67, `src/js/log.cpp` lines 209–213) resolves the variadic overload by formatting `label+args` first via `std::format`, then appending `' ({}ms)'` with the elapsed count. Callers that rely on the JS contract — placing format specifiers in `label` that consume both `...params` and the trailing elapsed value — will not produce identical output. Numeric formatting of elapsed itself is integer in both (matches `%d`).
- [ ] 38. [mmap.cpp] release_virtual_files() never deletes tracked MmapObjects, leaking every object created via create_virtual_file.
  - **JS Source**: `src/js/mmap.js` lines 28–45
  - **Status**: Pending
  - **Details**: In JS, `virtual_files` is a `Set` of GC-managed `MmapObject` instances; after `virtual_files.clear()` (line 40), V8's garbage collector eventually frees objects that no longer have other references, and the native finalizer in the addon releases native resources. In C++ `create_virtual_file` heap-allocates with `new MmapObject()` (`src/js/mmap.cpp` line 237) and inserts the raw pointer into the tracking set. `release_virtual_files` calls `unmap()` and `virtual_files.clear()` (lines 256, 263) but never invokes `delete` on any pointer, so every `MmapObject` ever created is leaked permanently — both the C++ object itself and any platform handles that were not explicitly unmapped before `clear()`. To match JS GC semantics the loop should `delete mmap_obj` (or store `std::unique_ptr<MmapObject>`) after unmapping.
- [ ] 39. [modules.cpp] Module register() callback object has no registerContextMenuOption hook — that path can never fire from within a module's register().
  - **JS Source**: `src/js/modules.js` lines 213–220
  - **Status**: Pending
  - **Details**: In JS `wrap_module` passes a `register_context` to `module_def.register()` exposing two callbacks: `registerNavButton(label, icon, install_types)` and `registerContextMenuOption(label, icon)` — the latter calls `register_context_menu_option(module_name, label, icon)`. C++ `wrap_module` (`src/js/modules.cpp` lines 207–219) just invokes `mod.registerModule()` with no callback object; modules call `register_nav_button` directly but there is no path through which a module's `register()` can register a context menu option. Any JS module that uses `this.registerContextMenuOption(...)` inside `register()` will silently never register that option in C++.
- [ ] 40. [modules.cpp] wrap_module captures display_label by value at registration time, defeating JS late-binding behaviour.
  - **JS Source**: `src/js/modules.js` lines 210, 214–216, 238–239
  - **Status**: Pending
  - **Details**: In JS `display_label` is a closure variable initialised to `module_name` and reassigned inside `registerNavButton` to the registered label; the wrapped `initialize()` reads it lazily, so any later re-registration that updates the label is reflected. C++ `wrap_module` (`src/js/modules.cpp` lines 208, 218, 229, 240–241) reads the label out of `nav_button_vec` right after `mod.registerModule()` and captures it by value into the lambda. If a module re-registers its nav button with a different label later, error toasts and log lines still use the stale label.
- [ ] 41. [modules.cpp] Wrapped initialize() catch only matches std::exception — leaves _tab_initializing permanently set on non-std-exception throws.
  - **JS Source**: `src/js/modules.js` lines 227–244
  - **Status**: Pending
  - **Details**: JS's wrapped `initialize` is `async` and uses `finally` to guarantee the `_tab_initializing` flag is cleared even when the awaited body rejects. C++ (`src/js/modules.cpp` lines 229–249) wraps the call in `try { ... } catch (const std::exception&)` and then assigns `_tab_initializing = true` after the catch. If `original_initialize` throws anything not derived from `std::exception` the assignment after the catch is unreachable and the guard remains permanently set, blocking all future activations.
- [ ] 42. [modules.cpp] set_active explicitly invokes activated() — JS relies on Vue keep-alive to call activated() outside set_active.
  - **JS Source**: `src/js/modules.js` lines 295–306
  - **Status**: Pending
  - **Details**: JS `set_active` only assigns `core.view.activeModule = active_module`. The `activated()` lifecycle hook is invoked by Vue's keep-alive system, not by `set_active`. C++ `set_active` (`src/js/modules.cpp` lines 557–558) explicitly calls `it->second.activated()` from inside `set_active`, meaning the wrapper that calls `initialize()` only triggers when `set_active` runs. Functionally similar but a deviation worth noting — if any code path activates a module without going through `set_active` (e.g. direct `ModuleDef::activated()`), the duplicate-call protection in JS (Vue keep-alive) is absent.
- [ ] 43. [modules.cpp] register_static_context_menu_option duplicates register_context_menu_option body and flattens the action shape.
  - **JS Source**: `src/js/modules.js` lines 164–166, 291–293
  - **Status**: Pending
  - **Details**: JS `register_context_menu_option(id, label, icon, action = null)` builds `{ id, label, icon, action }`. The static helper passes `{ handler: action, dev_only }` AS the `action` field, so callers consume `option.action.handler` and `option.action.dev_only`; `option.dev_only` does not exist on dynamically-registered options. C++ (`src/js/modules.cpp` lines 296–307, 322–342) flattens this into top-level `handler` + `dev_only` on every option and duplicates the registration body inside the static helper instead of delegating to `register_context_menu_option`. Any code reading `option.action.handler` or expecting `option.dev_only` to be undefined for non-static options sees a different shape; future changes to the registration path must also be made in two places.
- [ ] 44. [modules.cpp] reload_module log claims success but the C++ implementation cannot actually re-require the module.
  - **JS Source**: `src/js/modules.js` lines 339–356
  - **Status**: Pending
  - **Details**: JS `reload_module` clears the component_cache (preserving `EXCLUDE_FROM_RELOAD`), `delete modules[module_key]`, invalidates `require.cache[module_path]`, and re-requires the module before passing the fresh def to `wrap_module`. C++ `reload_module` (`src/js/modules.cpp` lines 606–616) deliberately skips re-requiring (impossible without a plugin system) and just resets `_tab_initialized=false` + re-wraps the existing `ModuleDef`. The function logs `"reloaded module: %s"` identically, suggesting reload happened, but the lambdas (render/mounted/registerModule/initialize) all still point at the same compiled code paths. The deviation is documented in code comments but the user-facing log message is misleading.
- [ ] 45. [modules.cpp] reloadAllModules does not re-add modules from the MODULES table — JS rebuilds the entire registry.
  - **JS Source**: `src/js/modules.js` lines 379–409
  - **Status**: Pending
  - **Details**: JS `reload_all_modules` deletes every entry from `modules`, then iterates `Object.entries(MODULES)` to fresh-require and wrap each one, fully rebuilding the registry. C++ `reloadAllModules` (`src/js/modules.cpp` lines 639–680) only iterates the existing `module_registry` to reset flags and re-wrap; it never re-runs the `add_module(...)` block from `initialize()`. Neither the C++ `reload_module` nor `reloadAllModules` ever erases from `module_registry`, so the `module_keys` collection at line 652 picks up the same modules that the loop at 662 reuses — fine for current behaviour but materially different from JS shape.
- [ ] 46. [modules.cpp] initialize() does not set manager — JS-visible side effect missing for module.exports / $modules.
  - **JS Source**: `src/js/modules.js` lines 282–283
  - **Status**: Pending
  - **Details**: JS `initialize` sets `manager = module.exports`, which is then exposed to every module via `module_def.computed.$modules = () => manager`. In C++ there is no `manager` variable and no `$modules` computed property — modules call `modules::set_active` etc. directly. `src/js/modules.cpp` `initialize()` (lines 349–531) does not set up any equivalent. Acceptable given the static-link design but means `this.$modules` from any ported module that still references it would be undefined.
- [ ] 47. [png-writer.cpp] write() does not await completion — returns a future immediately rather than blocking like JS `await`.
  - **JS Source**: `src/js/png-writer.js` lines 247–249
  - **Status**: Pending
  - **Details**: JS `async write(file) { return await this.getBuffer().writeToFile(file); }` awaits the file write before resolving its returned promise, so any caller using `await pngWriter.write(path)` is guaranteed the file is fully on disk (and any I/O error has thrown) before the next statement runs. The C++ port (`src/js/png-writer.cpp` lines 285–290) launches the write on `std::async(std::launch::async, ...)` and returns a `std::shared_future<void>` immediately; the file write proceeds on a background thread. Callers must explicitly call `.get()` to observe completion or errors, and the comment on line 281 acknowledges this. This breaks the JS guarantee that `await write(...)` synchronises with file completion — call sites that mirror the JS pattern of `pngWriter.write(path);` and continue will now race with the writer thread and silently drop write errors.
- [ ] 48. [subtitles.cpp] parse_sbt_timestamp uses a strict integer parser; JS parseInt + NaN propagation produces different malformed-input behaviour.
  - **JS Source**: `src/js/subtitles.js` lines 8–22
  - **Status**: Pending
  - **Details**: JS `parse_sbt_timestamp` calls `parseInt(parts[N], 10)`, which parses leading digits and ignores trailing non-numeric characters and returns `NaN` only when the leading character is non-numeric. The C++ helper `parse_sbt_int_js` (`src/js/subtitles.cpp` lines 21–42) returns `std::nullopt` whenever it encounters a non-digit, and `parse_sbt_timestamp` (lines 63–64) returns 0 if any component fails. JS `NaN` arithmetic propagates: a single `NaN` yields `NaN + NaN = NaN`, ultimately producing a `NaN` timestamp rather than 0. Additionally, JS `parseInt` returns 0 for `"00"` but `NaN` for empty strings — the C++ helper returns `nullopt` for empty strings, mapping to 0, which differs from JS's `NaN` propagation through the whole expression.
- [ ] 49. [subtitles.cpp] BOM check uses raw 3-byte UTF-8 BOM check rather than the JS `charCodeAt(0) === 0xFEFF` form.
  - **JS Source**: `src/js/subtitles.js` lines 177–178
  - **Status**: Pending
  - **Details**: JS `text.charCodeAt(0) === 0xFEFF` checks the first UTF-16 code unit of the decoded JS string. After `data.readString(undefined, 'utf8')` decodes the file as UTF-8, a leading UTF-8 BOM (EF BB BF) decodes to a single U+FEFF code point, so `charCodeAt(0)` returns 0xFEFF and `text.slice(1)` strips that one code point. The C++ implementation at `src/js/subtitles.cpp` lines 343–344 checks for the literal 3-byte UTF-8 BOM and strips 3 bytes. This is functionally equivalent for UTF-8 input. Documented as a deviation in the comment at lines 333–342, but per the project's fidelity rules deviations should only occur when "a direct port is genuinely impossible" — here the C++ result happens to match, so the deviation framing in the comment is misleading rather than functionally wrong.
- [ ] 50. [subtitles.cpp] get_subtitles_vtt uses getVirtualFileByID and exposes get_subtitles_vtt_from_text as an EXTRA public symbol.
  - **JS Source**: `src/js/subtitles.js` lines 172–187
  - **Status**: Pending
  - **Details**: JS `get_subtitles_vtt` is `async`, takes `(casc, file_data_id, format)`, calls `await casc.getFile(file_data_id)`, then `data.readString(undefined, 'utf8')`. C++ (`src/js/subtitles.cpp` lines 355–364) is synchronous, takes `(casc::CASC* casc, uint32_t file_data_id, SubtitleFormat format)`, calls `casc->getVirtualFileByID(file_data_id)` (not `getFile`), and calls `data.readString()` with no arguments (JS explicitly requests UTF-8). The sync-vs-async difference is the standard JS→C++ mapping. Two real deviations: (1) the C++ throws `std::runtime_error("casc is null")` on null `casc` (lines 356–357), which has no JS counterpart — JS would throw `TypeError` on the `.getFile` call, behaviourally similar but not identical; (2) `getVirtualFileByID` may resolve files differently than `casc.getFile` if it adds virtual-path resolution. Also `get_subtitles_vtt_from_text` is exposed as a public symbol in the header (line 44) but JS only exports `SUBTITLE_FORMAT` and `get_subtitles_vtt` (lines 189–192) — an EXTRA public symbol with no JS counterpart.
