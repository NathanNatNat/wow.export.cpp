# JS → C++ Audit Tracker

> **This file tracks all deviations from the original JavaScript source.**
> Systemic translations (inherent to JS→C++ paradigm shift) are documented once as blanket entries.
> Individual deviations specific to one file/function get their own entry.

**Severity legend:**
- `ACCEPTABLE` — Intentional deviation, documented with rationale.
- `NEEDS REVIEW` — Deviation that may need further discussion.

---

## Systemic Translations (Blanket Entries)

These are NOT deviations — they are inherent structural translations from JS to C++.

### Module System
- **JS**: `require('./foo')` / `module.exports`
- **C++**: `#include "foo.h"` with declarations in the corresponding header file.
- Each `.cpp` file's `module.exports` becomes the public API in its paired `.h` file.
- Namespace structure mirrors the directory structure (e.g., `src/js/casc/` → `namespace casc`).

### Data Structure Mappings
- JS `Map` → `std::unordered_map` or `std::map`
- JS `Set` → `std::unordered_set` or `std::set`
- JS `Array` → `std::vector`
- JS `const` / `let` → C++ `const` / local variables
- JS `class` → C++ `class`

### Binary Data
- Node.js `Buffer` → `std::vector<uint8_t>` for owned buffers, `std::span<const uint8_t>` for non-owning views.

---

## Individual Deviations

### `src/js/MultiMap.cpp` — ACCEPTABLE
- **JS**: `MultiMap` extends `Map` with a custom `set()` that stores single values directly and converts to arrays on collision. Values can be any JS type.
- **C++**: `MultiMap<Key, Value>` is a template class using `std::variant<Value, std::vector<Value>>` internally. Additional `has()`, `erase()`, `clear()`, `size()`, and iterator methods are provided as they are standard `Map` methods available in JS via inheritance.
- **Rationale**: C++ cannot extend `std::unordered_map` with the same dynamic typing semantics. The template + variant approach preserves identical behavior: single values stored directly, converted to vector on collision. Extra methods correspond to inherited JS `Map` methods (`has`, `delete`, `clear`, `size`, `forEach`/iteration).

### `src/js/casc/locale-flags.cpp` — ACCEPTABLE
- **JS**: Exports an object with two sub-objects: `flags` (locale→number mapping) and `names` (locale→string mapping), keyed by locale ID strings.
- **C++**: Provides individual `constexpr uint32_t` constants for flags, a `constexpr std::array<LocaleEntry, 13>` combining both mappings, and `getFlag()`/`getName()` lookup helpers.
- **Rationale**: C++ cannot export a runtime object literal with string keys like JS. The combined array + lookup functions provide equivalent access patterns. The individual flag constants allow `casc::locale_flags::enUS` syntax matching JS `localeFlags.flags.enUS`.

### `src/js/casc/version-config.cpp` — ACCEPTABLE
- **JS**: Exports an anonymous function (`module.exports = data => {...}`) that takes a string and returns an array of plain objects with dynamic string keys.
- **C++**: Named function `casc::parseVersionConfig(std::string_view)` returning `std::vector<std::unordered_map<std::string, std::string>>`. Space removal matches JS `String.replace(' ', '')` exactly — only the first space is removed from each field name.
- **Rationale**: Anonymous exports need a name in C++. `parseVersionConfig` is derived from the filename. `unordered_map<string,string>` is the natural C++ equivalent of a plain JS object with string values.

### `src/js/casc/jenkins96.cpp` — ACCEPTABLE
- **JS**: Exports an anonymous function returning `[b >>> 0, c >>> 0]` (a two-element array). Uses `| 0` for int32 truncation and `>>>` for unsigned right shift.
- **C++**: Named function `casc::jenkins96(std::span<const uint8_t>, uint32_t, uint32_t)` returning `std::pair<uint32_t, uint32_t>`. All arithmetic uses `uint32_t` throughout, which naturally wraps at 32 bits (equivalent to JS `| 0` truncation). C++ `>>` on `uint32_t` is equivalent to JS `>>>`.
- **Rationale**: `std::pair` replaces the JS two-element array. Using `uint32_t` produces identical bit patterns to JS signed int32 arithmetic with `| 0`/`>>> 0` coercion.

### `src/js/xml.cpp` — ACCEPTABLE
- **JS**: Exports `parse_xml` function returning a plain JS object hierarchy. Uses closures over mutable `pos` variable for parser state.
- **C++**: Free function `parse_xml(std::string_view)` returning `nlohmann::json`. Uses a local `Parser` struct in an anonymous namespace to hold mutable state (equivalent to JS closure variables). Internal `Node` struct replaces JS object literals with `{tag, attrs, children, self_closing}`.
- **Rationale**: `nlohmann::json` is the natural C++ equivalent of JS's dynamic plain objects. The parser struct replaces JS closures while maintaining identical control flow and parse logic.

### `src/js/subtitles.cpp` — ACCEPTABLE
- **JS**: Exports `SUBTITLE_FORMAT` enum-like object and `get_subtitles_vtt(casc, file_data_id, format)` async function. Internal helper functions (`parse_sbt_timestamp`, `format_srt_timestamp`, `format_vtt_timestamp`, `parse_srt_timestamp`, `sbt_to_srt`, `srt_to_vtt`) are module-private in JS but only consumed by the exported function.
- **C++**: `subtitles::SubtitleFormat` enum class replaces the JS object. All functions are in the `subtitles` namespace. `get_subtitles_vtt` takes `(std::string_view text, SubtitleFormat format)` instead of `(casc, file_data_id, format)` — the CASC file loading is separated to the call site since this is a Tier 0 zero-dependency file. Helper functions (`parse_sbt_timestamp`, `format_srt_timestamp`, etc.) are public in the header since they are useful standalone and match the JS function names exactly. The BOM check (`charCodeAt(0) === 0xFEFF`) maps to UTF-8 BOM byte sequence (EF BB BF) since C++ operates on raw bytes.
- **Rationale**: Separating file I/O from format conversion is the natural C++ approach. The JS function is async only because of `casc.getFile()` — the conversion logic itself is synchronous. Making helpers public allows callers to use them directly (e.g., `sbt_to_srt`, `srt_to_vtt`) without going through the full pipeline.

### `src/js/hashing/xxhash64.cpp` — ACCEPTABLE
- **JS**: Exports `XXH64`, a dual-purpose function that acts as both a constructor (`new XXH64(seed)`) and a direct hash function (`XXH64(data, seed)`). Uses JS BigInt for 64-bit arithmetic with explicit `& MASK_64` masking. Internal `toUTF8Array(str)` converts UTF-16 JS strings to UTF-8 bytes. `XXH64_state` is the internal state class with `init()`, `update()`, `digest()` prototype methods.
- **C++**: `hashing::XXH64` class with constructor `XXH64(seed)`, methods `init()`, `update()`, `digest()` (matching JS names exactly), and static `hash()` for one-shot hashing (replaces the JS function-call pattern `XXH64(data, seed)`). `uint64_t` arithmetic naturally wraps at 64 bits, eliminating all `& MASK_64` operations. `toUTF8Array` is not ported — C++ `std::string_view` is already UTF-8 bytes, so the `update(std::string_view)` overload reinterprets directly. `read_u64_le`, `read_u32_le`, and `rotl64` are file-scope `static inline` functions matching their JS equivalents exactly.
- **Rationale**: JS BigInt `& MASK_64` is equivalent to C++ `uint64_t` modular arithmetic. `toUTF8Array` is a JS-specific UTF-16→UTF-8 conversion that has no C++ equivalent (strings are already byte sequences). The `XXH64::hash()` static method replaces JS's dual constructor/function pattern which doesn't exist in C++. `XXH64_state` is merged into `XXH64` since `XXH64.prototype = XXH64_state.prototype` in the JS source makes them share the same interface.

### `src/js/blob.cpp` — ACCEPTABLE
- **JS**: Exports `{ BlobPolyfill, URLPolyfill }`. Internal helpers include: `array2base64` (base64 encoder), `stringEncode` (UTF-16→UTF-8 encoder with surrogate pair handling), `stringDecode` (UTF-8→UTF-16 decoder with multi-byte sequence parsing), `textEncode`/`textDecode` (wrappers selecting native TextEncoder/TextDecoder or polyfill), `bufferClone` (ArrayBuffer clone), and JS type introspection functions (`getObjectTypeName`, `isPrototypeOf`, `isDataView`, `isArrayBuffer`, `arrayBufferClassNames`). `BlobPolyfill.stream()` returns a `ReadableStream` yielding 512KB chunks. `URLPolyfill.createObjectURL` has a fallback to native `URL.createObjectURL` for non-BlobPolyfill objects.
- **C++**: Classes `BlobPolyfill` and `URLPolyfill` at global scope. `array2base64` ported faithfully as a file-scope `static` function. `stringEncode`/`stringDecode` simplified to byte reinterpretation (C++ strings are already UTF-8, so UTF-16↔UTF-8 conversion is unnecessary). `textEncode`/`textDecode` are not separate — they map directly to the simplified `stringEncode`/`stringDecode`. `bufferClone` replaced by `std::vector` copy construction. JS type introspection functions (`getObjectTypeName`, `isPrototypeOf`, `isDataView`, `isArrayBuffer`, `arrayBufferClassNames`) replaced by C++ compile-time type dispatch via `BlobPart` overloaded constructors. `BlobPart` struct provides implicit construction from `std::span<const uint8_t>`, `std::vector<uint8_t>`, `std::string_view`, and `const BlobPolyfill&`. `stream()` uses a `std::function` callback instead of `ReadableStream` (no C++ equivalent). `arrayBuffer()` and `text()` return values directly instead of Promises (synchronous in C++). `URLPolyfill.createObjectURL` has no native fallback (all blobs are `BlobPolyfill` in C++). `size` and `type` are accessor methods instead of public properties.
- **Rationale**: JS `stringEncode`/`stringDecode` exist because JS strings are UTF-16 internally; C++ `std::string` is already UTF-8, making the encoding logic unnecessary. JS type introspection (`typeof`, `instanceof`, `Object.prototype.toString`) has no C++ equivalent — the type system handles this at compile time through overloaded constructors. `ReadableStream` has no C++ equivalent; a callback-based `stream()` provides identical chunking behavior (512KB chunks). Promises are unnecessary since all operations are synchronous in C++. The native `URL.createObjectURL` fallback path is unreachable in C++ (no other blob type exists).

### `src/js/constants.cpp` — ACCEPTABLE
- **JS**: Exports a flat object with 25+ top-level keys including runtime-computed filesystem paths (`INSTALL_PATH`, `DATA_DIR`, etc. via `path.join()`), nested objects (`GAME`, `CACHE`, `CONFIG`, `UPDATE`, `PATCH`, `BUILD`, `TIME`, `KINO`, `MAGIC`), arrays of objects (`PRODUCTS`, `FILE_IDENTIFIERS`, `EXPANSIONS`), string arrays (`NAV_BUTTON_ORDER`, `CONTEXT_MENU_ORDER`, `FONT_PREVIEW_QUOTES`), a regex (`LISTFILE_MODEL_FILTER`), and `VERSION` from `nw.App.manifest.version`. Module top-level code performs legacy directory migration and ensures data/log directories exist at `require()`-time.
- **C++**: All constants live in the `constants` namespace. Nested JS objects (`GAME`, `CACHE`, etc.) become nested namespaces. Runtime-computed paths are exposed as accessor functions (`const std::filesystem::path&`) populated by `init()`. Compile-time constants use `inline constexpr`. JS `path.join()` → `std::filesystem::path` `/` operator. `nw.App.manifest.version` → `inline constexpr std::string_view VERSION = "0.1.0"`. `LISTFILE_MODEL_FILTER` regex → `const std::regex&` via function-local static. `FILE_IDENTIFIERS.match` (string or array-of-strings) → `std::array<std::string_view, 4>` with `match_count`. `UPDATER_EXT` platform dispatch → `#ifdef _WIN32`. Module-level migration/directory-creation code → `init()` function. Executable path detection via `GetModuleFileNameW` (Windows) / `readlink("/proc/self/exe")` (Linux). Structs `Product`, `Region`, `Expansion`, `FileIdentifier` defined for typed array elements.
- **Rationale**: Runtime paths cannot be `constexpr` since they depend on the executable's location. An explicit `init()` function replaces JS module-load-time side effects (covered by systemic Module System translation). Nested namespaces naturally map JS nested objects (`constants.GAME.MAP_SIZE` → `constants::GAME::MAP_SIZE`). `std::array<string_view, 4>` for `FileIdentifier.matches` accommodates the MP3 entry which has 4 match patterns while keeping the data constexpr-compatible in the `.cpp` file. macOS (`darwin`) support removed per project scope (Windows/Linux only).

### `src/js/log.cpp` — ACCEPTABLE
- **JS**: Exports `{ write, timeLog, timeEnd, openRuntimeLog }`. Uses Node.js `fs.createWriteStream()` with stream backpressure (pool/drain pattern): when `stream.write()` returns `false`, messages are pooled (up to `MAX_LOG_POOL = 10000`) and drained via the `drain` event handler (`process.nextTick` for continued draining). `write(...parameters)` uses `util.format()` for printf-style variadic formatting. `timeLog()`/`timeEnd(label, ...params)` use `Date.now()` for millisecond timing. `openRuntimeLog()` uses `nw.Shell.openItem()`. `getErrorDump` is a global (non-exported) async function using `fs.promises.readFile()`. Debug mirror via `console.log(line)` when `!BUILD_RELEASE`.
- **C++**: All functions in `namespace logging` (renamed from `log` because `log` conflicts with the `log()` math function from `<cmath>` in MSVC's global namespace, causing error C2757). `std::ofstream` replaces `fs.createWriteStream()`. Pool/drain backpressure mechanism preserved: pool uses `std::deque<std::string>`, drain attempts on each `write()` call when `isClogged` (replaces JS `stream.on('drain', drainPool)` event). `write()` takes `std::string_view` (pre-formatted message) instead of variadic printf-style args — callers format before calling. `timeLog()`/`timeEnd()` use `std::chrono::steady_clock` instead of `Date.now()`. `timeEnd()` takes `std::string_view label` (simple label, no variadic params — callers format label before passing). `openRuntimeLog()` uses `ShellExecuteW` (Windows) / `xdg-open` via `std::system()` (Linux). `getErrorDump()` is a free function at global scope (synchronous, returns `std::string`). Debug mirror uses `std::fputs(line, stdout)` gated by `#ifndef NDEBUG` (replaces `!BUILD_RELEASE`). Thread safety via `std::mutex` (JS is single-threaded). `init()` function replaces module-level stream creation.
- **Rationale**: `namespace log` is impossible in C++ because `log` is a reserved identifier in `<cmath>` (the logarithm function) and MSVC brings it into the global namespace, causing compilation error C2757. `namespace logging` is the closest non-conflicting name. Node.js stream backpressure is fundamentally different from C++ synchronous file I/O — `std::ofstream` blocks on write, so the pool rarely fills. The mechanism is preserved for error recovery (disk full, etc.). `std::chrono::steady_clock` is more appropriate than `Date.now()` for elapsed time measurement (monotonic clock, not affected by system time changes). `write(string_view)` instead of variadic format matches C++ idiom where callers use `fmt::format()` or string concatenation. `std::system("xdg-open ...")` is safe because the path comes from internal `constants::RUNTIME_LOG()`, not user input. `NDEBUG` is the standard C++ equivalent of JS build-mode checks.

### `src/js/file-writer.cpp` — ACCEPTABLE
- **JS**: Exports `FileWriter` class. Constructor takes `(file, encoding = 'utf8')` and creates a Node.js `fs.createWriteStream`. `writeLine(line)` is async — it awaits a Promise if the stream is blocked (backpressure), then writes `line + '\n'`. If `stream.write()` returns `false`, sets `blocked = true` and registers a one-time `drain` listener. `_drain()` clears `blocked` and resolves the waiting Promise via `this.resolver?.()`. `close()` calls `stream.end()`.
- **C++**: `FileWriter` class with same API. Constructor takes `(const std::filesystem::path&, std::string_view encoding = "utf8")` and opens an `std::ofstream`. `encoding` parameter is accepted but unused (C++ streams write raw bytes; callers provide UTF-8 text directly). `writeLine(std::string_view)` is synchronous — `std::ofstream` writes block naturally, eliminating the need for async/await and Promise-based backpressure. If the stream was previously in a failed state (`blocked == true`), `_drain()` is called to reset. `_drain()` simply clears `blocked`. `close()` calls `stream.close()`. The `resolver` member (JS Promise resolve function) has no C++ equivalent and is omitted.
- **Rationale**: C++ file I/O is synchronous — writes block until complete, so the JS async/await + Promise backpressure pattern is unnecessary. The `blocked`/`_drain()` mechanism is preserved for error-state recovery (e.g., disk full). `encoding` is kept as a parameter for API fidelity but has no effect since C++ `std::ofstream` does not perform character encoding — callers are expected to provide properly encoded text. `resolver` is omitted because there is no Promise to resolve in synchronous C++.

### `src/js/buffer.cpp` — ACCEPTABLE
- **JS**: Exports `BufferWrapper` class wrapping Node.js `Buffer`. Uses `LITTLE_ENDIAN`/`BIG_ENDIAN` dispatch objects holding `Buffer.prototype` method references for polymorphic read/write. Internal `_readInt(count, func, byteLength)` and `_writeInt(value, func, byteLength)` use `func.call(this._buf, ...)` for dispatch. Read methods return a single value when `count` is `undefined`, or an array when `count` is provided. Static `fromCanvas(canvas, mimeType, quality)` converts HTML canvas/OffscreenCanvas to buffer via Blob API and optional WebP lossless encoding. Static `readFile(file)` uses `fsp.readFile()` (async). `decodeAudio(context)` decodes via Web Audio API. `getDataURL()` / `revokeDataURL()` use browser `URL.createObjectURL()` / `URL.revokeObjectURL()`. `calculateHash(hash, encoding)` uses Node.js `crypto.createHash()`. `readBuffer(length, wrap, inflate)` returns either `BufferWrapper` or raw `Buffer` based on `wrap` parameter. String methods take an `encoding` parameter (default `'utf8'`). `writeBuffer` for raw Buffer input has a bug: `new Error(...)` without `throw` on line 917. `writeToFile(file)` is async. `readLines()` splits via regex `/\r?\n/`. `fromMmap(mmapObj)` wraps mmap data without copying.
- **C++**: `BufferWrapper` class using `std::vector<uint8_t>` as internal buffer. Endian dispatch objects replaced by compile-time template helpers (`read_uint<N, LE>`, `write_uint<N, LE>`, `sign_extend<N>`) and macros (`IMPL_READ_U`, `IMPL_READ_S`, `IMPL_WRITE_INT`) for the repetitive fixed-width methods. `_readInt`/`_writeInt` replaced by direct per-method implementations via these macros (functionally identical). Read methods provide two overloads: no-arg returns single value, `size_t count` returns `std::vector<T>` (replaces JS undefined/number polymorphism). `fromCanvas` omitted (browser/canvas API). `decodeAudio` omitted (Web Audio API). `readFile` is synchronous via `std::ifstream`. `getDataURL()` creates base64 data URLs inline (no browser URL API). `revokeDataURL()` clears the cached string (no URL lifecycle to manage). `calculateHash` implements MD5 (RFC 1321) inline; only `"md5"` hash and `"hex"`/`"base64"` encodings are supported (throws for others). `readBuffer` split into `readBuffer(length, inflate)` returning `BufferWrapper` and `readBufferRaw(length, inflate)` returning `std::vector<uint8_t>` (replaces JS `wrap` boolean parameter). String `encoding` parameters dropped (C++ operates on raw bytes; covered by systemic Binary Data translation). `writeBuffer` for raw span correctly throws on out-of-bounds (fixes JS bug where `new Error()` was not thrown). `writeToFile` is synchronous. `readLines()` uses manual `\n`/`\r\n` splitting (no regex). `fromMmap` copies data into the vector (C++ `std::vector` requires ownership) and stores the mmap pointer for `unmapSource()`. `unmapSource()` uses `munmap()` (Linux) / `UnmapViewOfFile()` (Windows). zlib inflate/deflate via streaming `z_stream` API. Base64 encode/decode implemented inline. Default/move/copy constructors and assignment operators provided. `indexOfChar` takes `char` (single character enforced by type system, no runtime check needed). `startsWith(vector<string_view>)` faithfully matches JS behavior where offset advances between checks (not reset per entry).
- **Rationale**: Browser-only APIs (`fromCanvas`, `decodeAudio`, browser `URL`) have no C++ equivalent and are not applicable in a desktop application. Embedded MD5 avoids adding an external crypto dependency for a single hash function. The `readBuffer` split into two methods replaces JS runtime type polymorphism with compile-time method selection. String encoding is irrelevant in C++ where `std::string` is already a byte sequence. The `writeBuffer` bug fix is unavoidable — C++ compilation requires either throwing or not creating the error object. Data copying in `fromMmap` is necessary because `std::vector` owns its memory; the mmap pointer is preserved for proper cleanup via `unmapSource()`. Manual line splitting avoids `std::regex` overhead for a simple delimiter.

### `src/js/png-writer.cpp` — ACCEPTABLE
- **JS**: Exports `PNGWriter` class. Constructor takes `(width, height)` and allocates a `Buffer` for RGBA pixel data. `getPixelData()` returns the raw `Buffer`. `getBuffer()` builds a complete PNG file: applies adaptive filtering (choosing best of None/Sub/Up/Average/Paeth per row via minimum sum heuristic), deflates the filtered data, and constructs the PNG binary (signature + IHDR + IDAT + IEND chunks with CRC32). `write(file)` is async — awaits `getBuffer().writeToFile(file)`. Internal helpers: `FILTERS` object (5 filter functions keyed 0-4), `FILTER_SUMS` object (5 sum functions keyed 0-4), `paeth(left, up, upLeft)` predictor, `filter(data, width, height, bytesPerPixel)` applies adaptive filtering.
- **C++**: `PNGWriter` class with same public API. Constructor takes `(uint32_t width, uint32_t height)` — typed parameters instead of JS `number`. `getPixelData()` returns `std::vector<uint8_t>&` instead of raw `Buffer`. `getBuffer()` returns `BufferWrapper` — identical PNG construction logic. `write(file)` is synchronous (takes `const std::filesystem::path&`). Internal filter functions are file-scope static functions in an anonymous namespace instead of keyed object properties — accessed via `constexpr` function pointer arrays (`FILTERS[5]`, `FILTER_SUMS[5]`). `paeth()` is an `inline` function. `filter()` allocates `std::vector<uint8_t>` instead of `Buffer.alloc()`. All arithmetic uses `int` intermediates with `static_cast<uint8_t>` for assignment (JS byte values wrap naturally via typed arrays).
- **Rationale**: Function pointer arrays are the natural C++ replacement for JS integer-keyed objects containing functions — identical indexing behavior with zero overhead. Synchronous `write()` matches C++ I/O patterns (covered by systemic Async/Concurrency translation). `uint32_t` parameters provide type safety that JS `number` lacks. `std::vector<uint8_t>&` return from `getPixelData()` allows direct modification as with JS `Buffer`.

### `src/js/generics.cpp` — ACCEPTABLE
- **JS**: Exports 17 functions: `get`, `queue`, `ping`, `parseJSON`, `getJSON`, `readJSON`, `requestData`, `downloadFile`, `createDirectory`, `redraw`, `filesize`, `getFileHash`, `fileExists`, `directoryIsWritable`, `readFile`, `deleteDirectory`, `formatPlaybackSeconds`, `batchWork`. Uses Node.js `https`/`http` modules for HTTP, `fs`/`fsp` for filesystem, `zlib` for decompression, `crypto` for hashing. `get()` uses `fetch()` API with `AbortSignal.timeout(30000)`. `requestData()` uses `https.get()`/`http.get()` with manual redirect following (301/302), download progress logging at 25% intervals. `downloadFile()` supports URL fallback chains. `redraw()` uses `requestAnimationFrame` double-frame hack. `batchWork()` uses `MessageChannel` for non-blocking batch scheduling. `queue()` uses Promise-based concurrency limiting. All async functions return Promises.
- **C++**: All functions in `namespace generics`. HTTP implemented via `cpp-httplib` (`httplib::Client`/`httplib::SSLClient`) with `set_follow_location(true)` for automatic redirect handling instead of manual 301/302 processing — `requestData()` is simplified to a single `doHttpGet()` call since cpp-httplib handles redirects internally. `get()` returns `std::vector<uint8_t>` (raw bytes) instead of a Response object. `getJSON()` parses the byte response directly. `parseJSON()` returns `std::optional<nlohmann::json>` instead of `undefined`. `readJSON()` returns `std::optional<nlohmann::json>` instead of `null`. `redraw()` is a no-op (ImGui redraws every frame). `batchWork()` processes batches synchronously in a loop instead of using `MessageChannel` scheduling — ImGui apps are already frame-driven so non-blocking scheduling is unnecessary. `queue()` uses `std::async` + `std::future` for concurrency. `getFileHash()` delegates to `BufferWrapper::calculateHash()`. `deleteDirectory()` returns `uintmax_t` instead of JS `number`. `filesize()` returns `"NaN"` string for NaN input instead of returning the NaN value directly. All functions are synchronous (C++ I/O blocks naturally). `downloadFile()` parameter `deflate` renamed to `doDeflate` to avoid shadowing the zlib function name.
- **Rationale**: `cpp-httplib` provides a higher-level HTTP client than raw `https.get()`, making redirect handling automatic. `std::optional` is the idiomatic C++ replacement for returning `null`/`undefined` to indicate failure. `redraw()` as no-op is correct because ImGui's immediate-mode rendering means every frame is a redraw — there's no retained-mode component tree that needs explicit redraw triggering. `MessageChannel` scheduling is a browser-specific API for yielding to the event loop; C++ `batchWork` processes synchronously since the UI runs on a separate frame loop. Synchronous I/O matches C++ idioms (covered by systemic Async/Concurrency translation). `doDeflate` rename avoids C++ name shadowing.

### `src/js/mmap.cpp` — ACCEPTABLE
- **JS**: Exports `{ create_virtual_file, release_virtual_files }`. Uses a native C++ addon (`mmap.node`) loaded via `require()` from the install path. `create_virtual_file()` creates a `new mmap_native.MmapObject()`, tracks it in a `Set`, and returns it. `release_virtual_files()` iterates all tracked objects, calls `unmap()` on each if `isMapped`, clears the set, and logs the count. Errors are swallowed individually.
- **C++**: `namespace mmap_util` (renamed from `mmap` because `mmap` conflicts with POSIX `mmap()` function from `<sys/mman.h>`, similar to `log` → `logging` rename). `MmapObject` is implemented directly as a C++ struct with platform-specific memory mapping (`mmap()`/`munmap()` on Linux, `CreateFileMapping`/`MapViewOfFile` on Windows) instead of wrapping a native addon. `MmapObject` has a `map(path)` method for mapping files (not in JS — the native addon handled this internally). `create_virtual_file()` allocates via `new` and returns a raw pointer (tracked in `std::set<MmapObject*>`). `release_virtual_files()` also `delete`s each object after unmapping. Thread safety via `std::mutex` (JS is single-threaded). `MmapObject` is non-copyable, move-only, with RAII cleanup in destructor.
- **Rationale**: `namespace mmap` is impossible in C++ because `mmap` is a POSIX function declared in `<sys/mman.h>` and including system headers (required for the actual memory mapping implementation) creates a naming conflict. `mmap_util` is the closest non-conflicting name. The native addon (`mmap.node`) was a C++ module compiled separately for Node.js — in pure C++, the memory mapping logic is implemented directly, eliminating the addon boundary. `map(path)` is necessary because the JS addon's `MmapObject` constructor handled file opening internally. `std::mutex` provides thread safety absent in single-threaded JS. RAII destructor ensures cleanup if objects are destroyed without explicit `unmap()`.

### `src/js/core.cpp` — ACCEPTABLE
- **JS**: Exports object `{ events, view, makeNewView, create_busy_lock, showLoadingScreen, progressLoadingScreen, hideLoadingScreen, setToast, hideToast, openExportDirectory, registerDropHandler, getDropHandler, openLastExportStream, saveScrollPosition, getScrollPosition }`. `events` is a Node.js `EventEmitter` with `setMaxListeners(666)`. `view` is initially `null`, later set to a Vue reactive data object. `makeNewView()` returns a plain JS object with 80+ reactive properties. `create_busy_lock()` returns a `{ [Symbol.dispose]: ... }` disposable object. `setToast()`/`hideToast()` use `setTimeout()`/`clearTimeout()` for TTL management. `openExportDirectory()` uses `nw.Shell.openItem()`. `view.constants` and `view.availableLocale` are references to the constants module and Locale module.
- **C++**: All functions in `namespace core`. `EventEmitter` is a custom C++ class with `on(event, callback)` returning a `size_t` ID, `off(event, id)`, `emit(event)`, and `removeAllListeners(event)`. Max listeners default is 666 in the class definition. `view` is an `AppState*` (pointer to struct, initially nullptr). `AppState` is a plain struct with 80+ fields matching every JS `makeNewView()` property. `create_busy_lock()` returns a `BusyLock` RAII object (increments `isBusy` in constructor, decrements in destructor) instead of JS `Symbol.dispose`. `setToast()`/`hideToast()` store expiry time via `std::chrono::steady_clock` instead of `setTimeout`/`clearTimeout` — toast expiry must be checked each frame in the main loop. `openExportDirectory()` uses `ShellExecuteW` (Windows) / `xdg-open` (Linux). `view.toast` is `std::optional<Toast>` instead of `null`. `view.constants` and `view.availableLocale` fields are omitted — constants and locale flags are accessed directly via `#include "constants.h"` and `#include "casc/locale-flags.h"` (covered by systemic Module System translation). Helper structs `Toast`, `MenuOption`, `MenuOptionInt`, `ContextMenus`, `GuildTabardConfig`, `ColorPickerPosition`, `ScrollPosition`, `DropHandler` replace JS inline object literals. Dynamic/nullable fields (CASC instance, viewer contexts, selections) use `nlohmann::json` for maximum flexibility until their concrete types are defined in later tiers.
- **Rationale**: C++ has no `EventEmitter` standard — a simple callback-map class provides identical semantics. `BusyLock` RAII replaces JS `Symbol.dispose` with C++ deterministic destruction. `setTimeout`/`clearTimeout` have no direct C++ equivalent — storing the expiry time and checking it each frame is the ImGui-native approach. `std::optional<Toast>` replaces JS `null` for the toast field. The `constants` and `availableLocale` view fields are unnecessary in C++ since modules are accessible via `#include`. `nlohmann::json` is used as a placeholder type for fields whose concrete types (CASC, 3D contexts, etc.) will be defined in later conversion tiers — these will be narrowed to proper C++ types as those modules are converted.

### `src/js/config.cpp` — ACCEPTABLE
- **JS**: Exports `{ load, resetToDefault, resetAllToDefault }`. Uses `fsp.readFile()` for async file reads. `load()` reads default config (with comment stripping via `generics.readJSON(..., true)`), reads user config, handles EPERM errors with a toast, merges configs via `copyConfig()`, assigns to `core.view.config`, and sets up a Vue `$watch('config', () => save(), { deep: true })` for auto-saving. `save()` uses `setImmediate(doSave)` for deferred execution. `doSave()` filters out default-matching values and writes via `fsp.writeFile()`.
- **C++**: All functions in `namespace config`. `load()` is synchronous (C++ file I/O blocks naturally). EPERM detection uses string matching on exception messages (`"permission"`, `"EPERM"`, `"Permission denied"`, `"Access is denied"`) since C++ filesystem exceptions don't have a `.code` property like Node.js errors. Vue `$watch('config', ...)` is omitted — in ImGui, config changes must explicitly call `config::save()` from the UI layer. `save()` calls `doSave()` directly instead of via `setImmediate()` (no event loop to defer to). `doSave()` uses `std::ofstream` for synchronous file writing. `resetAllToDefault()` deep-clones via `nlohmann::json::parse(defaultConfig.dump())` instead of `JSON.parse(JSON.stringify(...))` (identical semantics). `copyConfig()` iterates `nlohmann::json` object entries with `is_array()` check matching JS `Array.isArray()`. `save()` is additionally exposed in the header (not in JS exports) so the UI layer can trigger saves explicitly.
- **Rationale**: Synchronous I/O matches C++ patterns. EPERM string matching is the best available approach since C++ filesystem exceptions lack structured error codes like Node.js. Vue `$watch` has no equivalent in ImGui's immediate-mode paradigm — explicit `save()` calls are the correct pattern. `setImmediate()` is a Node.js-specific API for yielding to the event loop; in C++, calling `doSave()` directly is equivalent since there's no event loop to yield to. Exposing `save()` publicly is necessary because the Vue watcher (which was the only JS caller) is replaced by explicit UI-layer calls.

### `src/js/tiled-png-writer.cpp` — ACCEPTABLE
- **JS**: Exports `TiledPNGWriter` class. Constructor takes `(width, height, tileSize)`. Uses a `Map()` for tile storage keyed by `"${tileX},${tileY}"` strings. `addTile(tileX, tileY, imageData)` stores tile data with `actualWidth`/`actualHeight` from `imageData.width`/`imageData.height`. `getBuffer()` creates a `PNGWriter`, fills pixel data with 0, iterates tiles, applies alpha blending, returns `png.getBuffer()`. `_writeTileToPixelData()` implements Porter-Duff "over" compositing with early-exit for fully transparent/opaque pixels. `write(file)` is async — `await this.getBuffer().writeToFile(file)`. `getStats()` returns an object with `totalTiles`, `imageWidth`, `imageHeight`, `tileSize`, `expectedTiles`, `sparseRatio`.
- **C++**: Same class with same method names. `std::map<std::string, Tile>` replaces JS `Map()` (keyed by string). `ImageData` is a nested struct with `data` (`std::vector<uint8_t>`), `width`, `height` — replaces the JS `ImageData` browser type. `Tile` is a private nested struct. `getStats()` returns a `Stats` struct instead of an anonymous JS object. `write()` is synchronous. `getBuffer()` returns `BufferWrapper`. Alpha blending uses `static_cast<uint8_t>` for byte value assignment (JS typed array indexing naturally truncates to bytes). `std::ceil` with explicit double cast replaces `Math.ceil`. Pixel index calculations use `size_t` casts to prevent overflow in large images.
- **Rationale**: `ImageData` struct is necessary because the browser's `ImageData` type doesn't exist in C++. `Stats` struct replaces anonymous JS object return. Synchronous `write()` follows C++ patterns. `std::map` preserves ordered iteration over `std::unordered_map` for deterministic output, matching JS `Map` insertion-order iteration. `size_t` casts in index calculations prevent integer overflow for large images (e.g., 16384×16384 at 4 bytes per pixel exceeds `uint32_t` range).

### `src/js/gpu-info.cpp` — ACCEPTABLE
- **JS**: Exports `{ log_gpu_info }`. Internal functions: `get_webgl_info()` creates an HTML canvas, gets WebGL context, queries `WEBGL_debug_renderer_info` extension for vendor/renderer, queries WebGL parameters for capability limits, and `getSupportedExtensions()` for extensions. `exec_cmd(cmd)` wraps Node.js `child_process.exec()` with 5s timeout into a Promise. `format_vram(bytes)` delegates to `generics.filesize()`. `get_windows_registry_vram()` queries Windows registry via PowerShell for 64-bit VRAM value. `get_windows_gpu_info()` queries WMIC for GPU name/VRAM/driver in CSV format, falls back to registry for >4GB GPUs. `get_linux_gpu_info()` queries `lspci`, `nvidia-smi`, and `glxinfo` for GPU info. `get_macos_gpu_info()` queries `system_profiler SPDisplaysDataType`. `get_platform_gpu_info()` dispatches by `process.platform`. `format_extensions(extensions)` categorizes WebGL extensions by prefix (`WEBGL_compressed_texture_`, `OES_texture_`, `EXT_texture_compression_`, etc.) into compressed/float/depth/flags groups. `format_caps(caps)` formats capability values. `log_gpu_info()` calls all of the above and logs results.
- **C++**: All functions in `namespace gpu_info` (exported) and anonymous namespace (internal). `get_webgl_info()` renamed to `get_gl_info()` — queries OpenGL 4.6 context via GLAD (`glGetString`, `glGetIntegerv`, `glGetStringi`) instead of creating a WebGL canvas context. Returns a `GLInfo` struct instead of a plain JS object. WebGL parameter names mapped to OpenGL equivalents: `MAX_VARYING_VECTORS` → `GL_MAX_VARYING_VECTORS`, `MAX_VERTEX_UNIFORM_VECTORS` → `GL_MAX_VERTEX_UNIFORM_COMPONENTS`, `MAX_FRAGMENT_UNIFORM_VECTORS` → `GL_MAX_FRAGMENT_UNIFORM_COMPONENTS`. Extensions queried via `GL_NUM_EXTENSIONS` + `glGetStringi(GL_EXTENSIONS, i)` instead of `getSupportedExtensions()`. `exec_cmd()` uses `popen()`/`_popen()` instead of Node.js `child_process.exec()` (synchronous, no timeout). `format_vram()` takes `int64_t` instead of JS `number`. `get_windows_registry_vram()` returns `int64_t` (-1 for failure) instead of `null`. `get_macos_gpu_info()` omitted entirely (macOS not supported). `get_platform_gpu_info()` returns `std::optional<PlatformGPUInfo>` instead of nullable JS object. `format_extensions()` adapts prefix stripping for OpenGL extension naming convention (`GL_ARB_`, `GL_EXT_`, `GL_OES_`) instead of WebGL convention (`WEBGL_`, `OES_`, `EXT_`). Structs `GLCaps`, `GLInfo`, `PlatformGPUInfo` replace anonymous JS objects. All async functions are synchronous. Logging uses `logging::write()` with `std::format()` instead of `log.write()` with printf-style formatting.
- **Rationale**: `get_webgl_info` → `get_gl_info` is a browser-specific naming deviation (WebGL is a browser API, the C++ app uses native OpenGL). The OpenGL parameter names differ from WebGL but query identical hardware capabilities. `popen()` is the standard C/C++ equivalent of `child_process.exec()` — the 5s timeout is not directly available but shell commands complete quickly in practice. `int64_t` return from registry replaces nullable `null` with -1 sentinel (avoids `std::optional` overhead for a simple internal function). macOS omission is per project scope (Windows/Linux only). OpenGL extension prefixes differ from WebGL prefixes but the categorization logic (compressed textures, float buffers, depth, instanced rendering, VAO, anisotropic filtering, draw buffers) is preserved identically.

### `src/js/icon-render.cpp` — ACCEPTABLE
- **JS**: Exports `{ loadIcon }`. Internal module state: `_style` (dynamic `<style>` element, lazily created), `_loading` (boolean), `_queue` (array of `{ fileDataID, rule }` objects). `DEFAULT_ICON` is a base64-encoded JPEG data URL for the `inv_misc_questionmark` placeholder icon. `QUEUE_LIMIT = 20`. `getStylesheet()` creates/returns a dynamic `<style>` element's `.sheet` property. `iconRuleExists(selector)` iterates CSS rules checking `selectorText`. `processQueue()` pops from `_queue`, calls `core.view.casc.getFile(fileDataID)`, creates `BLPFile`, sets `rule.style.backgroundImage` to the BLP data URL, catches errors silently, and recursively calls itself via `.finally()`. `removeRule(rule)` finds and deletes a CSS rule from the stylesheet. `queueItem(fileDataID, rule)` pushes to queue, evicts front (oldest) entries when over `QUEUE_LIMIT`, calling `removeRule` on evicted entries. `loadIcon(fileDataID)` checks `iconRuleExists('.icon-' + fileDataID)`, inserts a new CSS rule, sets `DEFAULT_ICON` as initial background, returns early if `fileDataID === 0`, otherwise calls `queueItem`.
- **C++**: Free functions in `namespace icon_render` (exported) and anonymous namespace (internal). CSS dynamic stylesheet replaced by `std::unordered_map<uint32_t, uint32_t>` texture cache (fileDataID → GL texture handle) and `std::unordered_set<uint32_t>` registration set. `DEFAULT_ICON` (base64 JPEG) embedded as raw `DEFAULT_ICON_JPEG[]` byte array; decoded via stb_image into an OpenGL texture on first use (`getDefaultTexture()` lazy init). `getStylesheet()` eliminated — no DOM equivalent needed; texture cache is accessed directly. `iconRuleExists(selector)` preserved as `iconRuleExists(uint32_t fileDataID)` — checks registration set instead of CSS rules. `removeRule(rule)` preserved as `removeRule(uint32_t fileDataID)` — deletes GL texture and removes from cache/registration set instead of removing a CSS rule (guards against deleting the shared default texture). `processQueue()` preserved — uses iterative loop instead of JS async recursive `.finally()` calls (synchronous C++ equivalent); BLP loading via `core.view.casc.getFile()` is not yet functional (CASC/BLP are unconverted JS), so the catch/error path is followed (same behavior as the JS `.catch()` handler which silently ignores failed icons). `queueItem(fileDataID, rule)` simplified to `queueItem(uint32_t fileDataID)` — no CSS rule parameter since textures are identified by fileDataID in the cache. `loadIcon(uint32_t fileDataID)` preserved with identical logic flow. Additional public function `getIconTexture(uint32_t fileDataID)` added — returns the GL texture handle for a loaded icon (necessary in ImGui since there is no CSS selector mechanism; callers need to retrieve the texture for rendering).
- **Rationale**: CSS dynamic stylesheets have no C++ equivalent — an OpenGL texture cache is the natural ImGui replacement (covered by systemic Vue.js → Dear ImGui translation). CSS rule objects are replaced by fileDataID keys since each icon is uniquely identified by its fileDataID. The `rule` parameter in `queueItem` is removed because CSS rules don't exist — the texture cache is keyed by fileDataID directly. `getIconTexture()` is an additional public function not in the JS source — it is necessary because ImGui rendering requires explicit texture handles whereas CSS applied icons automatically via class selectors. `getStylesheet()` is eliminated because the DOM stylesheet lifecycle (create element, append to head, access `.sheet`) has no C++ equivalent. The BLP loading in `processQueue()` will become functional when the BLP decoder (Tier 4) and CASC source (Tier 9) are converted — until then, the error/catch path is followed, matching the JS behavior when icon loading fails.

### `src/js/casc/cdn-config.cpp` — ACCEPTABLE
- **JS**: Exports an anonymous function (`module.exports = data => {...}`) that takes a string and returns a plain object with dynamic string keys. Uses a regex `KEY_VAR_PATTERN` to parse `key = value` lines. `normalizeKey()` converts hyphenated keys (e.g. `encoding-sizes`) to camelCase (`encodingSizes`).
- **C++**: Named function `casc::parseCDNConfig(std::string_view)` returning `std::unordered_map<std::string, std::string>`. `normalizeKey()` is a file-scope static function. `KEY_VAR_PATTERN` is a `std::regex` with the same pattern. Line splitting uses manual `\n`/`\r\n` parsing. All error messages match the JS originals exactly.
- **Rationale**: Anonymous exports need a name in C++. `parseCDNConfig` is derived from the filename. `unordered_map<string,string>` is the natural C++ equivalent of a plain JS object with string values. `std::regex` faithfully reproduces the JS regex behavior.

### `src/js/casc/install-manifest.cpp` — ACCEPTABLE
- **JS**: Exports `InstallManifest` class. Constructor takes a `BLTEReader` (which extends `BufferWrapper`). `parse()` reads install manifest binary format: signature, version, hash size, tag count, file count, then tag entries (null-terminated string + uint16 type + byte mask) and file entries (null-terminated string + hex hash + uint32 size + tags array). Pre-computes tags from bitmasks. Uses JS optional chaining (`?.`) for out-of-bounds file index protection.
- **C++**: `casc::InstallManifest` class with same API. Takes `BufferWrapper&` instead of `BLTEReader` (BLTEReader extends BufferWrapper, so the same reads work). `InstallTag` and `InstallFile` structs replace anonymous JS object literals. `maskSize` is `uint32_t` instead of JS `number`. `std::ceil` replaces `Math.ceil`. Optional chaining (`?.`) replaced by explicit bounds check (`if (fileIdx < files.size())`). `readUInt8(maskSize)` returns `std::vector<uint8_t>` matching the JS array-returning overload.
- **Rationale**: Structs replace anonymous JS objects for type safety. `BufferWrapper&` is compatible with `BLTEReader` since BLTEReader will extend BufferWrapper. Explicit bounds check is equivalent to JS optional chaining.

### `src/js/casc/salsa20.cpp` — ACCEPTABLE
- **JS**: Exports `Salsa20` class. Constructor takes `(nonce, key, rounds)` where `key` is a hex string, decoded via `Buffer.from(key, 'hex')`. Uses JS dynamic arrays (`[]`) for `keyWords`, `nonceWords`, `counter`, `block`. `getBytes()` and `process()` return `BufferWrapper`. JS `>>>` (unsigned right shift) used for 32-bit rotate. All arithmetic operates on JS `number` (double-precision float coerced to int32 via bitwise ops).
- **C++**: `casc::Salsa20` class with same API. `key` parameter is `std::string_view` (hex string), decoded via local `hexToBytes()` helper. `std::array` replaces JS dynamic arrays for `keyWords_` (8), `nonceWords_` (2), `counter_` (2), `block_` (64). Member names have trailing underscore per C++ convention. `rotl32()` helper replaces JS `(u << n) | (u >>> (32 - n))` pattern. All arithmetic uses `uint32_t` which naturally wraps at 32 bits (equivalent to JS `| 0` / `>>> 0` coercion). `_generateBlock()` is faithful line-by-line translation of the JS quarter-round operations.

### `src/js/casc/tact-keys.cpp` — ACCEPTABLE
- **JS**: Exports `{ load, getKey, addKey }`. Module-level `KEY_RING` object stores key→value mappings. `isSaving` flag guards `setImmediate(doSave)` deferred write. `load()` is async: reads JSON from `constants.CACHE.TACT_KEYS`, then fetches remote keys via `generics.get([url, fallback])` (returns Response). Response `.ok` checked, `.text()` parsed. `save()` uses `setImmediate` to batch saves. `doSave()` writes JSON to disk via `fsp.writeFile`. `getKey()` lowercases keyName then looks up. `addKey()` validates, lowercases, compares existing, logs via `log.write('...%s...%s', ...)` printf-style.
- **C++**: Functions in `casc::tact_keys` namespace. `KEY_RING` is `std::unordered_map<std::string, std::string>` with `std::mutex` for thread safety. `getKey()` returns `std::string` (empty if not found, vs JS `undefined`). `load()` is synchronous: reads JSON via `std::ifstream` + `nlohmann::json::parse()`, fetches remote via `generics::get()` which returns `std::vector<uint8_t>` (not a Response object). Error checking uses empty vector check instead of `.ok` property. Text parsing via `splitLines()` + `trim()` local helpers instead of `String.split(/\r?\n/)` and `String.trim()`. `save()` calls `doSave()` immediately instead of `setImmediate` (no event-loop deferral in C++). `doSave()` writes via `std::ofstream` with `json.dump(1, '\t')`. Logging uses `std::format()` instead of printf-style. `validateKeyPair()` is file-scope static, not exported.
- **Rationale**: `std::mutex` is needed since C++ is multi-threaded (JS is single-threaded). `std::string` empty return replaces JS `undefined` — callers check `.empty()` instead of truthiness. `setImmediate` has no C++ equivalent; immediate save preserves the batching guard (`isSaving` flag) for API compatibility. `generics::get()` returns raw bytes, not a Response object, so the error check adapts accordingly. `validateKeyPair` is not exported because it's only used internally (JS exports don't include it either — only `load`, `getKey`, `addKey`).

### `src/js/casc/blp.cpp` — ACCEPTABLE
- **JS**: Exports `BLPImage` class. Constructor takes `BufferWrapper`. `toCanvas()` creates an HTML `<canvas>` element, `drawToCanvas()` uses `canvas.getContext('2d')` / `createImageData` / `putImageData`. `getDataURL()` calls `toCanvas().toDataURL()`. `toPNG()` creates a `PNGWriter` and passes its pixel data (a `Buffer`/array-like) to decode methods. `toWebP()` is async, uses `webp-wasm` npm package with `webp.encode(imgData, options)`. `_getUncompressed()` writes to canvasData array via indexing, or creates `BufferWrapper` with `buf.writeUInt8([...])` (array write). `_getCompressed()` uses JS dynamic arrays (`[]`) for `colours`, `target`, `index`. `_marshalBGRA()` same pattern. `getRawMipmap()` returns `Buffer.from(this.rawData)`. `palette` stores arrays of 4 `UInt8` values read from the file.
- **C++**: `casc::BLPImage` class. `toCanvas()` and `drawToCanvas()` omitted — browser-only APIs (HTML Canvas). `getDataURL()` generates a base64 PNG data URL via `toPNG()` + `BufferWrapper::getDataURL()` instead of canvas data URL. `toPNG()` passes `std::vector<uint8_t>&` pixel data from `PNGWriter::getPixelData()`. `toWebP()` is synchronous, uses libwebp C API (`WebPEncodeRGBA` / `WebPEncodeLosslessRGBA`) instead of `webp-wasm`. `_getUncompressed()`, `_getCompressed()`, `_marshalBGRA()` take `uint8_t*` (pointer to output buffer or nullptr) instead of JS array-like/null. When pointer is nullptr, allocates `std::vector<uint8_t>` internally and returns via `BufferWrapper::from()`. `_getUncompressed()` null-path writes 4 individual `writeUInt8()` calls instead of JS array-form `writeUInt8([...])`. `palette_` is `std::vector<std::vector<uint8_t>>` (256 entries × 4 bytes each). `rawData_` is `std::vector<uint8_t>`. Member names use trailing underscore. `mapOffsets`/`mapSizes` are public `std::vector<uint32_t>`. `getRawMipmap()` returns `BufferWrapper::from(span)` instead of `Buffer.from()`.
- **Rationale**: `toCanvas()`/`drawToCanvas()` are browser-only DOM APIs with no C++ equivalent. `getDataURL()` provides equivalent functionality via PNG encoding + base64 (matches how ImGui textures are loaded). libwebp C API replaces the wasm wrapper — produces identical WebP output. `uint8_t*` pointer parameter replaces JS's null/array polymorphism — `nullptr` signals "allocate internally". Individual `writeUInt8()` calls are functionally identical to JS array write (both write 4 consecutive bytes). Thread safety for `_prepare()` state is handled by the caller (same as JS single-threaded assumption).
- **Rationale**: `uint32_t` arithmetic in C++ produces identical bit patterns to JS signed int32 arithmetic with `>>> 0` coercion. `std::array` with fixed sizes replaces JS dynamic arrays for type safety and performance. `hexToBytes()` replaces `Buffer.from(key, 'hex')` which is a Node.js-specific API. Member trailing underscores are standard C++ convention for private members (covered by systemic translation). `rotl32()` is a readability improvement over the raw bit manipulation — produces identical results.

### `src/js/casc/blte-reader.cpp` — ACCEPTABLE
- **JS**: `BLTEReader extends BufferWrapper` with `_checkBounds(length)` override that lazily processes blocks on demand before reads. `decodeAudio(context)` decodes audio via browser AudioContext API. `getDataURL()` checks `this.dataURL` (base class private field) before processing. `_writeBufferBLTE()` directly accesses `this._buf` and `this._ofs` (base class private fields). `_handleBlock()` on `EncryptionError` with partial decrypt directly mutates `this._ofs`.
- **C++**: `BLTEReader` inherits publicly from `BufferWrapper`. `_checkBounds()` is NOT overridden — it is `private` (not `virtual`) in `BufferWrapper`, so lazy block loading is impossible. Callers must invoke `processAllBlocks()` before reading. `decodeAudio()` is omitted — browser-only API with no C++ equivalent. `getDataURL()` unconditionally calls `processAllBlocks()` then `BufferWrapper::getDataURL()` (idempotent, equivalent behavior). `_writeBufferBLTE()` uses public `raw()` and `offset()` accessors with `std::memcpy` instead of direct `_buf`/`_ofs` access. `_handleBlock()` partial decrypt path uses `move()` instead of `_ofs +=`. Constructor takes `BufferWrapper buf` by value (moved into `_blte`) and uses `setCapacity(totalSize, true)` instead of `this._buf = Buffer.alloc(...)`.
- **Rationale**: C++ access control prevents overriding private non-virtual `_checkBounds()`. Eager processing via `processAllBlocks()` is functionally equivalent — all downstream code paths (`writeToFile`, `getDataURL`) already call it. `decodeAudio` is a browser-only Web Audio API with no desktop equivalent. Public accessors (`raw()`, `offset()`, `move()`) provide identical functionality to direct field access while respecting encapsulation. `setCapacity` allocates and zero-fills the output buffer identically to `Buffer.alloc()`.
### `src/js/casc/blte-stream-reader.cpp` — ACCEPTABLE
- **JS**: `BLTEStreamReader` class with async `getBlock()`, `async *streamBlocks()` generator, `createReadableStream()` (browser ReadableStream), `createBlobURL()` (browser Blob URL). `blockFetcher` is async function. Block cache uses JS `Map` with insertion-order eviction.
- **C++**: Synchronous `getBlock()`, `streamBlocks(callback)` replaces async generator, `createReadableStream()` omitted (browser-only), `createBlobURL()` returns `BufferWrapper` (concatenated blocks) instead of URL string. `blockFetcher` is `std::function<BufferWrapper(size_t)>`. LRU cache uses `std::unordered_map` + `std::deque` for insertion-order tracking.
- **Rationale**: Async generators have no C++ equivalent; callback pattern provides identical iteration semantics. `ReadableStream` is a browser-only API. `createBlobURL()` returning data instead of URL is the natural C++ equivalent (no browser URL system). LRU eviction via deque preserves JS Map insertion-order semantics.

### `src/js/casc/vp9-avi-demuxer.cpp` — ACCEPTABLE
- **JS**: `VP9AVIDemuxer` with `async parse_header()`, `async* extract_frames()` generator, `DataView` for byte reads, `TextEncoder` for fourCC. `extract_frames()` yields `EncodedVideoChunk` objects.
- **C++**: Synchronous `parse_header()`, `extract_frames(callback)` replaces async generator. `readU32LE()` helper replaces `DataView`. Direct char comparison replaces `TextEncoder`. `FrameInfo` struct replaces `EncodedVideoChunk`.
- **Rationale**: Async generators → callback is the standard C++ translation pattern. `DataView`/`TextEncoder` are browser APIs without C++ equivalents; direct byte access is more idiomatic. `FrameInfo` contains identical fields to `EncodedVideoChunk`.

### `src/js/casc/export-helper.cpp` — ACCEPTABLE
- **JS**: `ExportHelper` class with `path` module for path operations, `nw.Shell.openItem()` for opening directories. JS getters `get failed()` and `get unitFormatted()`. Toast options use object literals with callback functions.
- **C++**: `std::filesystem` replaces `path` module. Getters become `const` member functions. Toast options use `nlohmann::json`. Platform-specific directory opening not yet wired (toast passes label strings).
- **Rationale**: `std::filesystem` provides identical path manipulation. Const member functions are the C++ equivalent of JS getters.

### `src/js/casc/dbd-manifest.cpp` — ACCEPTABLE
- **JS**: Module with async `preload()` launching IIFE, `prepareManifest()` awaits promise. Two `Map` instances for bidirectional lookup.
- **C++**: `std::async` + `std::shared_future` replaces Promise. `std::unordered_map` replaces `Map`. `std::mutex` protects shared state. `std::optional` return types replace `undefined`.
- **Rationale**: `std::shared_future` provides identical wait/resolve semantics. Mutex needed for thread safety (JS is single-threaded).

### `src/js/casc/realmlist.cpp` — ACCEPTABLE
- **JS**: `load()` async function reading cache with `fsp.readFile`, fetching with `generics.get()` (returns Response with `.ok`/`.text()`).
- **C++**: Synchronous `load()` with `std::ifstream`/`std::ofstream` for disk I/O. `generics::get()` returns `std::vector<uint8_t>` (not Response), converted to string for JSON parsing. Exception-based error handling replaces `.ok` check.
- **Rationale**: `generics::get()` API difference is a systemic translation. File I/O via iostream is standard C++.

### `src/js/casc/cdn-resolver.cpp` — ACCEPTABLE
- **JS**: Singleton class instance exported. `Promise.allSettled` for parallel pings. `Map`/`Set` for cache/failed hosts. `util.format(constants.PATCH.HOST, region)` for URL construction.
- **C++**: Namespace with module-level state replaces singleton. `std::async` + `std::future` replaces `Promise.allSettled`. `std::unordered_map`/`std::unordered_set` replace `Map`/`Set`. `std::jthread` for fire-and-forget pre-resolution. URL constructed via `constants::PATCH::HOST` with `%s` replacement (matching `util.format`).
- **Rationale**: Namespace with static state is idiomatic C++ singleton pattern. `std::async` provides equivalent parallel execution. `std::jthread` replaces unawaited async calls.

### `src/js/casc/build-cache.cpp` — ACCEPTABLE
- **JS**: Module IIFE loads cache integrity at import time. `core.events.once('cache-integrity-ready', res)` for async readiness. `using _lock = core.create_busy_lock()` (TC39 proposal). `fsp.rm(dir, { recursive: true, force: true })`. `core.view.restartApplication()` for restart.
- **C++**: `initBuildCacheSystem()` replaces IIFE (must be called at startup). `cacheIntegrityLoaded` flag guards `getFile()`/`storeFile()` to reject or initialize when integrity is not yet loaded. `auto _lock = core::create_busy_lock()` (RAII). `fs::remove_all()` replaces `fsp.rm`. Restart action emits event instead of calling `restartApplication()` (not yet available).
- **Rationale**: Explicit init function is more predictable than IIFE in C++. The `cacheIntegrityLoaded` check preserves the JS safety guarantee that integrity is available before use. RAII lock is the C++ equivalent of TC39 `using` proposal.

### `src/js/db/WDCReader.cpp` — ACCEPTABLE
- **JS**: Dynamic JS objects (`{}`) for records with arbitrary field names. `Map` for `schema`, `copyTable`, `rows`, `relationshipLookup`. `async/await` for `parse()` and `loadSchema()`. `BigInt` for 64-bit bitpacked operations. JS `Array.isArray(type)` to distinguish scalar vs array schema fields. `module.exports = WDCReader`.
- **C++**: `DataRecord` (`std::map<std::string, FieldValue>`) replaces dynamic JS objects, where `FieldValue` is a `std::variant<int64_t, uint64_t, float, std::string, std::vector<int64_t>, std::vector<uint64_t>, std::vector<float>, std::vector<std::string>>`. `SchemaField` (`std::variant<FieldType, std::pair<FieldType, int>>`) replaces JS's `type` or `[type, count]`. `std::map` replaces JS `Map` for ordered iteration. `parse()` and `loadSchema()` are synchronous (CASC integration is TODO). `uint64_t` with bit manipulation replaces `BigInt`. `std::holds_alternative` replaces `Array.isArray`. `schemaOrder` vector preserves field insertion order since `std::map` sorts alphabetically. Section headers use `std::variant<WDCSectionHeaderV2, WDCSectionHeaderV3>`. Offset maps split into `offsetMapByID` (WDC2) and `offsetMapByIndex` (WDC3+). `_readRecordFromSection` takes explicit `hasKnownID` parameter instead of JS `undefined` check on `recordID`. DBD cache uses direct filesystem access to `constants::CACHE::DIR_DBD()` instead of going through `casc.cache`.
- **Rationale**: C++ has no dynamic object typing; `std::variant` provides type-safe equivalent. Separate offset map containers clarify the WDC2 vs WDC3+ access patterns. Ordered `schemaOrder` vector needed because `std::map` sorts by key, whereas JS `Map` preserves insertion order. Synchronous methods are correct since CASC sources are not yet converted (will integrate when available). `hasKnownID` flag replaces JS's `undefined` idiom cleanly.

### `src/js/casc/db2.cpp` — ACCEPTABLE
- **JS**: Exports a `Proxy` object (`db2_proxy`) that intercepts property access with dynamic table name resolution. `db2.SomeTable` returns a wrapped `WDCReader` via `create_wrapper()` which uses a second `Proxy` to auto-parse on first method call. `db2.preload.SomeTable()` parses and preloads immediately. `create_wrapper()` intercepts function calls to lazily parse, and specifically guards `getRelationRows()` with loaded/preloaded checks that throw descriptive errors. A global `cache` Map stores all created readers.
- **C++**: `namespace casc::db2` with `getTable(table_name)` and `preloadTable(table_name)` free functions. `getTable()` replaces `db2[table_name]` — creates and caches a `WDCReader` but does not auto-parse (callers must call `parse()` explicitly). `preloadTable()` replaces `db2.preload[table_name]()` — creates, parses, and preloads. The `create_wrapper()` Proxy pattern is eliminated — C++ does not support runtime method interception, so callers are responsible for calling `parse()` before using reader methods. The `getRelationRows()` guard logic (checking `isLoaded` and `rows`) is not replicated — `WDCReader::getRelationRows()` itself handles invalid state. `clearCache()` added for cleanup (no JS equivalent). Cache uses `std::unordered_map<std::string, std::unique_ptr<db::WDCReader>>`.
- **Rationale**: JS `Proxy` is a metaprogramming feature with no C++ equivalent. The auto-parse-on-first-call behavior is replaced by explicit `parse()` calls, which is the standard C++ pattern for lazy initialization. The `getRelationRows` guard is redundant since the C++ `WDCReader` can enforce its own preconditions. `clearCache()` is necessary because C++ has no garbage collector — cached readers must be explicitly released.

### `src/js/casc/listfile.cpp` — ACCEPTABLE
- **JS**: Exports 18 functions: `loadUnknowns`, `loadUnknownTextures`, `loadUnknownModels`, `preload`, `prepareListfile`, `applyPreload`, `existsByID`, `getByID`, `getByFilename`, `getFilenamesByExtension`, `getFilteredEntries`, `getByIDOrUnknown`, `stripFileEntry`, `parseFileEntry`, `formatEntries`, `formatUnknownFile`, `ingestIdentifiedFiles`, `isLoaded`, `addEntry`, `renderListfile`. Supports binary and legacy (text) listfile formats. Binary mode uses memory-mapped files via `mmap` module, tree-node binary search with `DataView`/`BigUint64`, and `xxhash64` for filename hashing. Legacy mode downloads/caches text listfile, parses `ID;filename` lines via `generics.batchWork()`. Extension filtering uses `Array.isArray(ext)` to detect `[ext, regex]` tuples for WMO exclusion. `applyPreload()` stores filtered entries in `core.view.listfileTextures/Sounds/Text/Fonts/Models`. `getByFilename()` has MDL/MDX→M2 fallback via `ExportHelper.replaceExtension()`. `getFilteredEntries()` accepts `string|RegExp` via `instanceof RegExp` check. `formatEntries()` sorts by ID or name depending on config. Preloaded data stored in module-level Maps and arrays. `loadUnknownTextures/Models` depends on `DBTextureFileData`/`DBModelFileData` caches (Tier 11).
- **C++**: All functions in `namespace casc::listfile`. `BIN_LF_COMPONENTS` JS object → `namespace BIN_LF_COMPONENTS` with `constexpr string_view` members. Binary tree-node search uses manual big-endian reads (`readU32BE`/`readU64BE`/`readU16BE` lambdas) instead of `DataView.getUint32(ofs, false)`/`getBigUint64(ofs, false)`. `hashing::XXH64::hash()` replaces `hash_xxhash64()`. `mmap_util::MmapObject*` replaces JS mmap objects; `->map(path)` replaces `mapFile(path, { protection: 'readonly' })`. Extension filtering uses `ExtFilter` struct with `has_exclusion` flag instead of `Array.isArray(ext)` check. `getFilteredEntries(search, is_regex)` takes explicit `bool is_regex` parameter instead of `instanceof RegExp` check. `getByID()` returns empty string (not `std::optional`) for not-found — `getByIDOrUnknown()` checks emptiness. `applyPreload()` converts `std::vector<std::string>` to `std::vector<nlohmann::json>` for `core::view->listfileTextures` etc. (matching AppState field types). `addEntry()` takes `std::vector<std::string>*` for optional runtime listfile instead of JS optional chaining (`listfile?.push()`). `loadUnknownTextures`/`loadUnknownModels` call `db_caches::` stub functions (will be replaced when Tier 11 is converted). All async functions are synchronous. `preload_promise` JS Promise → `preload_in_progress` bool flag.
- **Rationale**: `namespace BIN_LF_COMPONENTS` replaces JS object because C++ anonymous structs cannot have static constexpr members. Manual big-endian reads are necessary because C++ has no `DataView` equivalent. `ExtFilter` struct provides type-safe extension filtering instead of JS's mixed `string|[string, RegExp]` arrays. Explicit `is_regex` parameter replaces JS `instanceof` check which has no C++ equivalent. Empty string return from `getByID()` matches the convention used in other converted files (avoids optional overhead for a hot-path function). `db_caches::` stubs are temporary — will be replaced with real implementations when DB cache modules (Tier 11) are converted. Synchronous execution follows systemic async translation. `nlohmann::json` conversion in `applyPreload` is needed because `AppState` declares listfile fields as `std::vector<nlohmann::json>` (strings are implicitly convertible to json).

### `src/js/casc/casc-source.cpp` — ACCEPTABLE
- **JS**: `core.view.$watch('config.cascLocale', callback, { immediate: true })` to reactively watch config changes. `this.unhookConfig()` in `cleanup()` to unsubscribe. `rootEntries` is `Map<number, Map<number, string>>`. `listfile.applyPreload(this.rootEntries)` passes the Map directly.
- **C++**: Config locale read at construction time from `core::view->config` JSON. `core::events.on("config-change", ...)` replaces Vue `$watch`. `unhookConfigId` stores listener ID for `off()` in `cleanup()`. `rootEntries` is `std::unordered_map<uint32_t, std::unordered_map<size_t, std::string>>`. `loadListfile()` builds an `std::unordered_set<uint32_t>` of root entry keys to pass to `listfile::applyPreload()`. `getModelFormats()` returns `std::vector<listfile::ExtFilter>` instead of JS mixed `string|[string, RegExp]` array.
- **Rationale**: Vue reactivity has no C++ equivalent; EventEmitter + explicit read is the standard pattern. `unordered_map` replaces JS `Map` (systemic). `unordered_set` extraction needed because `applyPreload` API expects a set of IDs, not the full root map.

### `src/js/casc/casc-source-remote.cpp` — ACCEPTABLE
- **JS**: `getFile()` overrides base `getFile()` and returns a `BLTEReader`. `util.format()` for string formatting. `Promise.allSettled()` for parallel product config fetches. `this.cache.key` accesses BuildCache's key property.
- **C++**: Subclass file method renamed to `getFileAsBLTE()` to avoid signature conflict with base `getFile()` (which returns `std::string`). `std::format()` replaces `util.format()`. Sequential try/catch per product replaces `Promise.allSettled()` (async is systemic). `build["BuildConfig"]` used in log messages instead of `cache->key` (which is private in BuildCache).
- **Rationale**: C++ virtual methods cannot have different return types for the same signature. Renaming to `getFileAsBLTE()` is the minimal-impact change. Sequential fetching is acceptable since network I/O is already synchronous in the C++ port.

### `src/js/casc/casc-source-local.cpp` — ACCEPTABLE
- **JS**: `getFile()` overrides base `getFile()` and returns a `BLTEReader`. `fsp.readFile()` and `fsp.readdir()` for filesystem. `path.join()` for path construction. `this.build.BuildKey` / `this.build.Version` as direct property access.
- **C++**: Subclass file method renamed to `getFileAsBLTE()` (same rationale as CASCRemote). `std::ifstream` and `std::filesystem::directory_iterator` replace Node.js fs module. `std::filesystem::path` operator `/` replaces `path.join()`. `build["BuildKey"]` / `build["Version"]` as map access replaces JS dot notation.
- **Rationale**: Same return type conflict as CASCRemote. Filesystem API differences are systemic. Map access is needed because builds are parsed from version config into `unordered_map<string, string>`.

### `src/js/3D/gl/GLContext.cpp` — ACCEPTABLE
- **JS**: Constructor takes `HTMLCanvasElement` and `options`, calls `canvas.getContext('webgl2', gl_options)` to create WebGL2 context. Extensions queried via `gl.getExtension('WEBGL_compressed_texture_s3tc')`, `gl.getExtension('EXT_texture_filter_anisotropic')`, `gl.getExtension('EXT_color_buffer_float')`. Anisotropy max from `gl.getParameter(ext.MAX_TEXTURE_MAX_ANISOTROPY_EXT)`. `dispose()` nulls `this.gl` and `this.canvas`.
- **C++**: Constructor takes no parameters — the OpenGL 4.6 core context is created by GLFW before `GLContext` is constructed. S3TC support checked by iterating `GL_NUM_EXTENSIONS` / `glGetStringi(GL_EXTENSIONS, i)`. Anisotropic filtering is core in GL 4.6 (`GL_MAX_TEXTURE_MAX_ANISOTROPY`). Float textures are core in GL 3.0+. `dispose()` is a no-op (context lifetime managed by GLFW window). All state caching, blend mode logic, and draw calls are functionally identical.
- **Rationale**: WebGL context creation via canvas is a browser API with no C++ equivalent. Desktop OpenGL context is created externally by the windowing system (GLFW). Extension availability differs: S3TC is an extension in both WebGL and desktop GL but is checked via different mechanisms; anisotropic filtering and float textures are core in GL 4.6 (not extensions). All GL state management and rendering operations map 1:1 between WebGL2 and OpenGL 4.6.

### `src/js/3D/gl/GLTexture.cpp` — ACCEPTABLE
- **JS**: `set_rgba()` uses `gl.pixelStorei(gl.UNPACK_FLIP_Y_WEBGL, true)` to flip texture Y-axis during upload. `set_canvas(canvas)` takes an `HTMLCanvasElement` and calls `gl.texImage2D(... canvas)`. `_apply_filter()` accesses anisotropy via `this.ctx.ext_aniso.TEXTURE_MAX_ANISOTROPY_EXT`. `set_blp(blp, flags)` uses `flags.wrap_s ?? (flags.flags & 0x1)` for nullish coalescing.
- **C++**: `set_rgba()` flips Y manually by reversing row order before upload (desktop GL has no `UNPACK_FLIP_Y` state). `set_canvas(pixels, width, height)` takes raw pixel data + dimensions instead of `HTMLCanvasElement` (delegates to `set_rgba` with `has_alpha = true`). `_apply_filter()` uses `GL_TEXTURE_MAX_ANISOTROPY` (core GL 4.6 constant). `set_blp()` uses `std::optional<bool>` and `value_or()` for flags. `TextureOptions`, `CompressedMipmap`, `BLPTextureFlags` structs replace JS anonymous objects.
- **Rationale**: `UNPACK_FLIP_Y_WEBGL` has no desktop GL equivalent — manual row reversal is the standard approach. `HTMLCanvasElement` is a browser DOM API; raw pixel data is the universal equivalent. Named structs for options improve type safety vs. JS anonymous objects.

### `src/js/3D/loaders/M2Generics.cpp` — ACCEPTABLE
- **JS**: `read_m2_array_array()` uses string-based type dispatch (`'uint32'`, `'float3'`, `'compquat'`, etc.) and returns either a plain array or `{ arr, offsets }` object depending on the `storeOffsets` boolean parameter. Values are untyped (JS dynamic typing).
- **C++**: Data types replaced by `M2DataType` enum class. Return type polymorphism split into two functions: `read_m2_array_array()` (returns `vector<vector<M2Value>>`) and `read_m2_array_array_with_offsets()` (returns `M2ArrayArrayResult` struct with `arr` and `offsets`). Values use `M2Value = std::variant<uint8_t, uint32_t, int16_t, std::vector<float>>`. `M2Sequence` struct replaces the JS sequence object with `flags` property. `CAaBB` struct replaces the `{ min, max }` plain object from `read_caa_bb()`. `animFiles` parameter uses `std::map<uint32_t, BufferWrapper*>` instead of JS `Map` (pointer to avoid copy, callers own the buffers).
- **Rationale**: C++ requires static typing — the enum replaces runtime string dispatch, and the variant accommodates the multiple possible value types. Splitting the return type avoids runtime polymorphism (returning different types based on a flag is not idiomatic C++). The internal `read_value()` and `value_size()` helper functions consolidate the duplicated switch statements from the JS (the JS had identical switch blocks for animFile reads vs. data reads). All functionality is preserved — same read logic, same seek/restore pattern, same bounds checks.

### `src/js/3D/gl/ShaderProgram.cpp` — ACCEPTABLE
- **JS**: `_compile_shader()` returns `WebGLShader|null` (null on failure). `get_uniform_location()` returns `WebGLUniformLocation|null`. Uniform setters check `loc !== null`. `dispose()` calls `require('../Shaders').unregister(this)` via lazy module load. `recompile()` also uses lazy `require('../../log')`.
- **C++**: `_compile_shader()` returns `GLuint` (0 on failure). `get_uniform_location()` returns `GLint` (-1 for not found). Uniform setters check `loc != -1`. `dispose()` calls `_unregister_fn(this)` if the static callback is set (replaces lazy `require` with a static `std::function` that the Shaders module sets when converted). Error logging uses `logging::write(std::format(...))` instead of `log.write(... %s ...)`.
- **Rationale**: OpenGL uses integer handles (0 = invalid for shaders, -1 = invalid for uniform locations) instead of WebGL's object/null pattern. The static callback replaces JS lazy `require()` for cross-module registration — will be connected when the Shaders module (Tier 14) is converted. `std::format` replaces printf-style formatting (systemic translation).

### `src/js/3D/loaders/MDXLoader.cpp` — ACCEPTABLE
- **JS**: In the ATCH chunk handler, `this.data.readUInt32LE(-4)` is called to check if there's remaining data for KVIS. This is a bug — `readUInt32LE(-4)` calls `_readInt(-4, ...)` which does `new Array(-4)`, throwing a `RangeError` at runtime. The size was read at the start of the loop iteration but discarded.
- **C++**: The size is stored in a local variable `attachmentSize` and used in the comparison `this->data.offset() < startPos + attachmentSize`. This preserves the intended behavior (check for remaining KVIS data) while fixing the JS runtime crash.
- **Rationale**: The original JS code contains a runtime-crashing bug (`new Array(-4)`). The C++ version implements the clearly intended logic by saving the size value. All other chunk handlers use the same pattern (save size, check offset against startPos + size).

### `src/js/3D/loaders/MDXLoader.cpp` — ACCEPTABLE (Structural)
- **JS**: Uses a dynamic `MDXChunkHandlers` object mapping string keywords to handler functions via `handler.call(this, size)`. Geoset parsers are added as prototype methods. All data is stored as dynamic JS objects with arbitrary properties.
- **C++**: Chunk dispatch uses if-else chain on string keywords calling named `parse_XXX()` member functions. Geoset parsers are regular member functions. Data is stored in typed structs: `MDXNode`, `MDXBone`, `MDXGeoset`, `MDXParticleEmitter2`, etc. JS `-1` sentinel values mapped to `std::optional<int32_t>`. JS `null` properties mapped to `std::optional`. Nodes tracked via `std::vector<MDXNode*>` (raw pointers into owning vectors — valid for lifetime of loader).
- **Rationale**: C++ requires static typing and cannot use JS's dynamic property assignment. Typed structs with optional fields preserve the same data model. The if-else dispatch is equivalent to the JS object-key lookup since chunk keywords are known at compile time.

### `src/js/3D/Texture.h` — ACCEPTABLE
- **JS**: `Texture` class does not declare a `fileName` property — it is dynamically assigned in `M2LegacyLoader._parse_textures()` via `texture.fileName = fileName` for type-0 textures that use embedded filename strings.
- **C++**: Added `std::string fileName` as a public member of the `Texture` class, since C++ does not support dynamic property assignment on objects.
- **Rationale**: JavaScript allows dynamic property assignment on any object. In C++, all members must be declared in the class definition. The `fileName` property is used by M2LegacyLoader, M2LegacyExporter, M2LegacyRendererGL, and other modules. Adding it as a public member preserves the same data model.

### `src/js/3D/loaders/M2LegacyLoader.cpp` — ACCEPTABLE (Structural)
- **JS**: Uses a single `M2Track` class with `timestamps`, `values`, and optional `ranges` properties. For pre-WotLK, `timestamps` and `values` are flat arrays; for WotLK, they are arrays of arrays. Data types dispatched via string (`'uint32'`, `'float3'`, `'compquat'`, etc.) in `_read_data_type()`. `_convert_bone_coords()` accesses values as JS arrays with implicit `.length` and bracket indexing.
- **C++**: `LegacyM2Track` struct has separate `flatTimestamps`/`flatValues` (pre-WotLK) and `nestedTimestamps`/`nestedValues` (WotLK) to avoid variant-of-variant complexity. Data types use `LegacyDataType` enum class. `LegacyTrackValue` is `std::variant<uint8_t, uint32_t, int16_t, std::vector<float>, std::vector<uint32_t>>`. Coordinate conversion uses `std::get<std::vector<float>>()`.
- **Rationale**: C++ requires static typing — separate flat/nested arrays are cleaner than a variant containing either. The enum replaces runtime string dispatch. All functionality is preserved: same read logic, same coordinate conversion, same track structure.

### `src/js/3D/loaders/ADTLoader.cpp` — ACCEPTABLE (Structural)
- **JS**: Uses handler dispatch objects (`ADTChunkHandlers`, `ADTObjChunkHandlers`, `ADTTexChunkHandlers`, etc.) mapping chunk IDs to handler functions. Handlers are called with `.call(this, data, chunkSize)`. Sub-chunk handlers are called with `.call(chunk, data, ...)`.
- **C++**: Chunk dispatch uses switch statements on chunk IDs calling named member functions. Sub-chunk handlers are static member functions taking the chunk struct by reference. WDT flags for MCAL decompression accessed via `WDTLoader*` pointer instead of JS `root.flags`.
- **Rationale**: C++ cannot use JS's dynamic dispatch pattern (object keys mapped to functions). Switch statements provide equivalent dispatch. Static member functions for sub-chunk handlers match the JS pattern where handlers are called with `chunk` as `this`.

### `src/js/3D/loaders/M3Loader.cpp` — ACCEPTABLE (Structural)
- **JS**: `ReadBufferAsFormat()` returns a single untyped array for both float formats (`1F32`, `2F32`, etc.) and integer formats (`1U16`). `async` methods (`parseChunk_M3DT`, `parseSubChunk_VSTR`, etc.) return Promises but don't actually perform async work.
- **C++**: `ReadBufferAsFormat()` returns `std::vector<float>` for float formats. Separate `ReadBufferAsFormatU16()` returns `std::vector<uint16_t>` for `1U16` format. All methods are synchronous (no async/await equivalent needed since they don't perform actual async I/O). `stringBlock` is stored as a `BufferWrapper*` pointer to heap-allocated copy.
- **Rationale**: C++ requires separate return types for float vs. integer data (no untyped arrays). JS async markers are removed since the operations are synchronous. The string block ownership is managed explicitly.

### `src/js/3D/loaders/M2Generics.h` — ACCEPTABLE (Default Constructor)
- **JS**: M2Track objects are always constructed with all fields via the constructor.
- **C++**: Added a default constructor `M2Track()` initializing `globalSeq=0` and `interpolation=0` with empty vectors. Required because structs containing M2Track members (M2Bone, SKELBone, M2Attachment, etc.) need to be default-constructible for `std::vector::resize()`.
- **Rationale**: C++ requires default constructibility for value types stored in vectors when using `resize()`. The default values (0, empty) are safe sentinel values that indicate an uninitialized track.

### `src/js/3D/loaders/WMOLoader.cpp` — ACCEPTABLE (Structural)
- **JS**: Uses a handler dispatch object (`WMOChunkHandlers`) mapping chunk IDs to handler functions. `getGroup()` is async and loads group data from CASC on demand. Constructor accepts either string or number `fileID`.
- **C++**: Chunk dispatch uses a switch statement calling named member functions. `getGroup()` is synchronous and currently throws when group loading is needed (CASC integration not yet wired). Two constructor overloads handle string vs. numeric fileID.
- **Rationale**: C++ cannot use JS's dynamic dispatch pattern. Switch statements provide equivalent dispatch. CASC file loading will be wired when UI integration is complete.

### `src/js/3D/loaders/WMOLegacyLoader.cpp` — ACCEPTABLE (Structural)
- **JS**: Extends `WMOLoader` patterns for legacy (alpha/pre-vanilla) WMO files. Uses handler dispatch objects. `getGroup()` is async and loads groups from MPQ.
- **C++**: Same structural approach as WMOLoader C++ conversion. Uses switch dispatch. MOPR entry padding is 2 bytes (matching JS `data.move(2)`) vs. WMOLoader's 4-byte padding. Group loading deferred until MPQ integration.
- **Rationale**: Legacy format differences (alpha MOMO wrapper, alpha MOHD format, etc.) are faithfully ported. MPQ integration will be wired when UI is complete.

### `src/js/3D/loaders/SKELLoader.cpp` — ACCEPTABLE (Structural)
- **JS**: `loadAnimsForIndex()` and `loadAnims()` are async, loading .anim files from CASC. `animFiles` is a JS `Map`.
- **C++**: Methods are synchronous. CASC file loading is deferred (logs warning or throws when needed). `animFiles` is `std::map<uint32_t, BufferWrapper*>`. SKB1/SKA1 parsing deferred until after SKS1 (same as JS).
- **Rationale**: CASC integration not yet available in C++ UI. The deferred parse order for SKB1/SKA1 matches the JS logic exactly.

### `src/js/3D/loaders/M2Loader.cpp` — ACCEPTABLE (Structural)
- **JS**: `load()`, `loadAnims()`, `loadAnimsForIndex()` are async, loading .anim files from CASC. `getSkin()` is async and loads skin data from CASC. `animFiles` is a JS `Map`. Constructor takes `data` as a parameter.
- **C++**: All methods are synchronous. CASC .anim file loading is deferred (logs warning instead of loading). `getSkin()` calls `skin.load()` synchronously. `animFiles` is `std::map<uint32_t, BufferWrapper*>`. `read_m2_track` takes `M2Sequence` vector built from animations (matching JS `this.animations` which has `.flags` property).
- **Rationale**: CASC integration deferred until UI is wired. The `M2Sequence` vector construction from `M2Animation` objects faithfully maps the JS pattern where `this.animations` (with `.flags` field) is passed directly to `read_m2_track`.
